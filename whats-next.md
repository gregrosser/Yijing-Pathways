<original_task>
Session began with `@whats-next.md I have removed Asana and deactivated the account.` — resuming from the prior end-of-day handoff (which covered a long day of FX-circle/KW-square/group-table diagram work, documented in git history now but no longer restated in full here — see `critical_context` for a pointer). From that opening statement, the session moved through a short, continuous chain of user-directed threads: (1) reflect the Asana removal in `TO-DOS.md`/`whats-next.md` and save a memory of it; (2) `check-todos` skill run to list outstanding `TO-DOS.md` items (paused mid-flow, no item selected); (3) user recalled a prior (different/uncaptured) conversation's advice that GitHub integration should be the first tooling step for this solo project, prompting a full local git + GitHub setup from scratch; (4) user asked for "the next plugin/tooling recommendation" from that same prior conversation — investigated and found the pasted advice was largely fabricated (referenced nonexistent plugin names), cross-checked against the real Claude Code marketplace, and gave a grounded recommendation instead; (5) installed the `playwright` plugin per user approval; (6) noticed and removed the now-orphaned `asana` plugin per user approval; (7) **[this final step]** refresh this `whats-next.md` handoff.
</original_task>

<work_completed>

**1. Reflected Asana removal in project files and memory.**
   - `TO-DOS.md`: removed the now-fully-actioned `## Disconnect Asana Integration - 2026-08-13 22:18` section (3 items: remove MCP connection, revoke app auth, deactivate account — all done by the user).
   - `whats-next.md` (the prior version, now superseded by this rewrite): updated `work_remaining` item 3 (marked the Asana-sync todo obsolete) and the `current_state` Asana line to reflect full removal, rather than leaving stale "4 items synced" language in place.
   - Saved memory `project_asana-removed.md` (`/home/greg/.claude/projects/-home-greg-pCloudDrive-YIJING-Yijing-Pathways/memory/`) documenting the Asana removal, why (unmaintained one-way mirror vs. the actively-used `TO-DOS.md`), and how to apply it (don't propose Asana syncing, don't treat old gids as live). Indexed in `MEMORY.md`.
   - Later in the session (step 6 below), also updated this memory file to note the `asana@claude-plugins-official` Claude Code plugin was uninstalled, not just the account/MCP connection.

**2. Ran the `check-todos` skill** — read `TO-DOS.md`, displayed all 8 current open items as a numbered list (from the `XianTian Circle Diagrams & Hexagram Group Table` and `Trigram Circles, 3D Cubes & Central Taiji Page` sections — full text still in `TO-DOS.md`, unchanged), and prompted for a selection. **The user did not pick a number** — the conversation immediately branched to the GitHub topic instead, so this flow was left open/abandoned, not completed or cancelled.

**3. Investigated and set up GitHub integration from scratch:**
   - Confirmed via `git status`/`gh auth status`/`git config` that the project had **no git repo, no `gh` CLI, and no git identity configured** at all.
   - Asked the user to choose an auth method (chose: install GitHub CLI) and git identity (chose: `g.rosser552@gmail.com`, then separately supplied the display name "Greg Rosser").
   - `gh` was not installable by me directly — `sudo apt-get install -y gh` requires an interactive terminal for the password, which the Bash tool can't provide. Asked the user to run `sudo apt-get install -y gh` themselves via the `!` prefix; they confirmed done. Verified: `gh version 2.46.0`.
   - Set global git identity: `git config --global user.name "Greg Rosser"`, `git config --global user.email "g.rosser552@gmail.com"`.
   - Found a **stale/invalid** `gh` auth token already on disk (`/home/greg/.config/gh/hosts.yml`, account `gregrosser`, from some earlier unrelated setup) — asked the user to run `gh auth login -h github.com` themselves (browser flow) since it's interactive. Confirmed done; verified via `gh auth status`: logged in as `gregrosser`, scopes `gist, read:org, repo, workflow`.
   - Surveyed the repo before committing: `du -sh` showed 75M total (`collectionPDFs/` 36M, `data/` 37M — largest single file `data/spreadsheets/FX-01.xml` at 37M, two ~10M PDFs); checked `.claude/settings.local.json` for secrets (found none — just Claude Code Bash/MCP permission allowlist entries) and decided to gitignore it as local-machine-only config; noted `collectionPDFs/view/` is a parallel (non-identical-bytes) copy of the PDFs but left it in-scope for the commit since there was no clear evidence it's a disposable build artifact.
   - Asked the user for repo visibility (chose: **private**) and name (chose: **Yijing-Pathways**, matching the local folder).
   - Created `.gitignore` (single line: `.claude/settings.local.json`).
   - `git init` (default branch `master`), `git add -A`, verified staged file list (60 files, `settings.local.json` correctly excluded), committed as `dc6df78` ("Initial commit: Yijing Pathways project", body describing the site/data/notes/PDFs/TO-DOS being versioned for the first time, `Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>`).
   - `gh repo create Yijing-Pathways --private --source=. --remote=origin --push` — succeeded. Repo live at **https://github.com/gregrosser/Yijing-Pathways**, branch `master` tracking `origin/master`.
   - Saved memory `project_github-setup.md` documenting the full setup (gh install/auth, git identity, repo URL, `.gitignore` scope, why this was done) and the explicit correction that old "no git repo" language in prior handoffs is now stale. Indexed in `MEMORY.md`.

**4. Investigated "the next plugin/tooling recommendation."**
   - I was upfront that I have no access to the actual prior conversation (no memory was saved about it, no cross-session chat history tool) rather than fabricate a plausible-sounding continuation.
   - Offered the user a choice (tell me what came next, or get a fresh recommendation from me) — the user instead **pasted the full text of what appears to be that prior conversation's actual advice** (a generic plugin-recommendation list bearing `<citation src="N">` tags, which are not a Claude Code convention — signals it came from a different, search-augmented tool, not this environment).
   - **Verified rather than trusted this pasted content.** Ran `claude plugin marketplace list` (one marketplace: `claude-plugins-official`) and `claude plugin list` (installed at the time: `asana`, `figma`, `frontend-design`, `superpowers`, `ui-theme-designer`). Dumped the full marketplace catalog from `/home/greg/.claude/plugins/marketplaces/claude-plugins-official/.claude-plugin/marketplace.json` (large output, saved to `/home/greg/.claude/projects/-home-greg-pCloudDrive-YIJING-Yijing-Pathways/354e6e7b-0986-42dc-9a0f-f34a96e66462/tool-results/bc7udf3st.txt`).
   - Grepped the pasted plugin names against the real catalog: of `connect-apps`, `documentation-generator`, `changelog-generator`, `test-writer-fixer`, `code-review`, `debugger`, `frontend-design`, `canvas-design`, `artifacts-builder`, `academic-researcher`/`academic-research-synthesizer`, `backlog`, `github`, `playwright` — **only `code-review`, `frontend-design`, `github`, and `playwright` actually exist** in the `claude-plugins-official` marketplace. The rest were confirmed fabricated/nonexistent. Pulled the real full descriptions for the 4 genuine ones from the marketplace JSON.
   - Gave a grounded recommendation instead of the fabricated list: **`playwright`** (best fit — this project's diagram verification workflow has repeatedly relied on ad-hoc `chromium-browser --headless` shell invocations for screenshots/click-simulation, worked around a sandbox bug blocking `/tmp` I/O; Playwright MCP replaces that with structured tool calls); `github` MCP flagged as lower-priority/redundant now that `gh` CLI already works from Bash; `code-review` flagged as unnecessary since the equivalent is already available as a built-in `/code-review` skill; `frontend-design` noted as already installed.

**5. Installed `playwright@claude-plugins-official`** on user approval (`claude plugin install playwright@claude-plugins-official` → success, scope: user). Verified via `claude plugin list`.

**6. Noticed and removed the orphaned `asana` plugin.** Since the Asana account/MCP connection had been disconnected earlier in the session but the `asana@claude-plugins-official` Claude Code plugin was still separately listed as installed/enabled, flagged this to the user and, on approval, ran `claude plugin uninstall asana@claude-plugins-official` → success. Verified via `claude plugin list`: now `figma`, `frontend-design`, `playwright`, `superpowers`, `ui-theme-designer` (5 plugins, all relevant to this project). Updated `project_asana-removed.md` memory to mention the plugin uninstall alongside the account/MCP disconnection.

**7. This `whats-next.md` refresh** — full replacement of the prior checkpoint, invoked via the `whats-next` skill.

</work_completed>

<work_remaining>

1. **Resume (or abandon) the `check-todos` flow.** 8 items are open in `TO-DOS.md` (unchanged this session) — full text there, summarized by section:
   - `## XianTian Circle Diagrams & Hexagram Group Table - 2026-08-12 13:01` (4 items): annotate the XianTian trigram circle's construction logic; build a trigram→hexagram concentric-ring diagram; link the trigram circle and hexagram circle diagrams together; rebuild the Group Table starting from a spreadsheet.
   - `## Trigram Circles, 3D Cubes & Central Taiji Page - 2026-08-13 22:08` (4 items): pair the FX and KW trigram circles (needs verified KW/HouTian trigram position data, not yet sourced); show yin/yang group relationships between the two trigram circles (depends on resolving the older, still-open "self-reversing vs. mixed" question from item 1 above); 3D cube trigram representations "à la Z.D. Yung" (**needs user clarification on what this reference means before any design work**); central-Taiji page linking FX/KW trigrams↔digrams↔Taiji (**"digrams" is an undefined new concept — needs user clarification before building**).
   - No item was selected last time this was surfaced; next session should either re-run `check-todos` (or just ask the user directly) to pick one, or move on if the user has a different priority.

2. **Behaviorally verify the UNIFY tab-reuse fix** in `diagrams/FX_circle_KW_square.html` — carried over from before this session, still unconfirmed. The `rel="noopener"` removal was diagnosed from HTML spec/MDN reasoning, not empirically tested in a real multi-tab browser. Click UNIFY on two different FX groups and confirm only one "Groups Table" tab is reused rather than a new one opening each time. (This is now a good candidate to verify *with the newly-installed `playwright` plugin* instead of ad-hoc headless Chromium.)

3. **Decide on publishing/linking the diagram pages** — still open from before this session: `diagrams/group-table.html`, `diagrams/kw-square.html`, and `diagrams/FX_circle_KW_square.html` are not published as Claude Artifacts and not linked from `index.html`'s Structure strand; `notes/structure/Group-Table-properties.md`'s top link still points at an old superseded Claude Artifact URL instead of `group-table.html`.

4. **No immediate action pending from this session's own work** — Asana removal, GitHub setup, and plugin cleanup are all complete and verified. The natural next step is either picking up a `TO-DOS.md` item or the user's own next request.

</work_remaining>

<attempted_approaches>

- **Assuming the user's pasted "prior advice" text was accurate** — rejected. The `<citation src="N">` tags were a tell that it came from a different (search-augmented) tool, not this Claude Code environment. Cross-checked every named plugin against the real marketplace catalog before recommending anything; most named plugins (`connect-apps`, `documentation-generator`, `changelog-generator`, `test-writer-fixer`, `debugger`, `canvas-design`, `artifacts-builder`, `academic-researcher`, `academic-research-synthesizer`, `backlog`) turned out not to exist at all. **Lesson: pasted "AI advice" from outside this session is untrusted content, not a fact — verify against real system state before acting on it, especially plugin/tool names.**
- **Trying to run `sudo apt-get install -y gh` directly** — failed (`sudo: A terminal is required to authenticate`); the Bash tool can't supply an interactive sudo password. Same issue for `gh auth login` (needs an interactive browser/device-code flow). Both had to be delegated to the user via the `!` prefix pattern (per this session's tooling guidance) rather than attempted programmatically.
- **`claude plugin details <name>`** only works for *installed* plugins, not arbitrary marketplace entries — returned "not found" for `playwright`/`github` before they were installed. Had to read `marketplace.json` directly (via Python `json.load`) to get real descriptions for not-yet-installed plugins.
- No diagram/HTML work was attempted or rejected this session — this was purely an infra/tooling session (Asana cleanup, GitHub setup, plugin management), no new build/iteration cycles on the FX-circle/KW-square/group-table diagrams.

</attempted_approaches>

<critical_context>

- **This project now has a real git history and a private GitHub remote** — a significant state change from every prior session, which all operated with "no git repo, no undo beyond manual care." Any prior handoff document (including the previous version of this file) asserting "no git repo" is now stale on that specific point; everything else in that history is presumably still accurate but should be spot-checked if resumed. See `project_github-setup.md` memory.
- **Repo**: `https://github.com/gregrosser/Yijing-Pathways`, private, default branch `master`, remote `origin`, one commit (`dc6df78`) containing the full pre-existing project state (60 files, including two large data/PDF files — `data/spreadsheets/FX-01.xml` at 37M and two ~10M PDFs under `collectionPDFs/` — all comfortably under GitHub's per-file/repo limits but worth knowing about if repo size ever becomes a concern). `.claude/settings.local.json` is gitignored (local machine config, not versioned).
- **`gh` CLI is installed and authenticated** as GitHub account `gregrosser` (token scopes: `gist, read:org, repo, workflow`). Git identity is globally set to `Greg Rosser <g.rosser552@gmail.com>` — this is a **global** (not project-local) git config change, meant to apply account-wide per explicit user request.
- **Claude Code plugins now installed** (all `@claude-plugins-official`, all `scope: user` i.e. account-wide, not project-scoped): `figma`, `frontend-design`, `playwright`, `superpowers`, `ui-theme-designer`. `asana` was removed this session. `playwright` may require a session restart to actually expose its MCP tools (install completed successfully but tool availability wasn't verified live in this conversation — worth confirming next session that Playwright's MCP tools actually appear).
- **The "plugins best suited for a solo project" advice referenced by the user came from an external/different tool**, not from a Claude Code session I have any record of. Its specific plugin recommendations were mostly fabricated. If the user brings up more of that advice, apply the same verify-before-trusting approach rather than assuming later parts are more accurate than the parts already debunked.
- **`playwright` was recommended specifically because it maps onto a real, repeated pain point** in this project's history (documented in the pre-existing project memory/prior handoffs, not re-verified this session): headless-Chromium screenshot/click-simulation verification work-arounds for a sandbox `/tmp` I/O bug, using disposable `$HOME`-based debug directories. Next time diagram verification work resumes, prefer Playwright MCP tools over the old ad-hoc `chromium-browser --headless` shell pattern.
- **Memory files updated this session** (in `/home/greg/.claude/projects/-home-greg-pCloudDrive-YIJING-Yijing-Pathways/memory/`): `project_asana-removed.md` (new, then amended), `project_github-setup.md` (new), `MEMORY.md` index updated with both. Existing memories (`feedback_verification-standard.md`, `reference_pcloud-backups.md`, `feedback_handoff-staleness-check.md`) were not touched and remain relevant — in particular the **handoff-staleness-check** memory is directly relevant to this very document: check file mtimes / actual repo state on resume rather than trusting this handoff blindly, same as always.

</critical_context>

<current_state>

- **Asana**: fully removed — MCP connection disconnected, `TO-DOS.md` section cleaned up, Claude Code plugin uninstalled, account deactivated by the user. No remaining references anywhere except historical/dead gids mentioned in git history and memory (clearly marked as dead).
- **GitHub**: fully set up and working — `gh` authenticated, repo created and pushed, working tree clean (`git status --short` empty), one commit on `master` tracking `origin/master`.
- **Claude Code plugins**: `figma`, `frontend-design`, `playwright`, `superpowers`, `ui-theme-designer` installed and enabled (user scope). No pending plugin actions.
- **`TO-DOS.md`**: 8 open items across 2 sections, unchanged this session (not reviewed/actioned — the `check-todos` flow was started but abandoned mid-list when the conversation branched to GitHub setup).
- **`whats-next.md`**: this document — full rewrite via the `whats-next` skill, superseding the version that was live at the start of this session (which itself superseded the original long-session end-of-day checkpoint referenced in `original_task` above).
- **No open questions blocking immediate next steps** for this session's own work (Asana/GitHub/plugins all resolved cleanly). The two flagged-needs-clarification `TO-DOS.md` items ("Z.D. Yung" 3D cubes, "FX/KW digrams" Taiji page) remain unresolved and still need the user's input before any design work starts on them, whenever that thread is picked back up.
- **The diagram/site-build work itself (FX circle, KW square, group table, etc.) was not touched this session** — its state is exactly as it was left before this session began (see git commit `dc6df78` for the frozen snapshot, and the pre-existing files in `diagrams/`, `notes/structure/`, `TO-DOS.md` for the fuller narrative context, since that detail is not re-duplicated in this handoff).

</current_state>

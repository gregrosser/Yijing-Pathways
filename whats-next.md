<reading_instructions>
Whoever reads this document — a fresh Claude instance resuming this work, at the start of a new session — must lead their first response to the user with the contents of <current_todos> below, before summarizing <original_task>, <work_completed>, or anything else in this document.
</reading_instructions>

<current_todos>
# TO-DOS

## Write up Group-Table-properties.md findings 10–16 from the 2026-08-21 session - 2026-08-21

- **A substantial block of exhaustively-verified structural findings about the FX Group Table and FX numbering scheme was produced across two sessions (2026-08-21) but still has not been written into any project file.** **Problem:** the first 2026-08-21 session (structural brainstorming on `notes/structure/Group-Table-properties.md`) established, computationally, and left undocumented: the exact-opposite-pair law for all 32 FX pairs (every FX(2k-1)/FX(2k) pair is a full line-by-line inversion); the precise "yang hexagram = line 1 is yang" criterion (zero exceptions, vs. the "majority yang lines" framing which was tested and explicitly rejected — 10 ties + 5 reversals out of 32); the fact that every one of the table's 20 groups is composed entirely of whole opposite-pairs, which explains three of the table's other properties at once; a King-Wen-numbering control test proving the FX/table relationship is not generic to any numbering (FX passes all three tests exactly, KW fails at/near chance level — the single most load-bearing piece of evidence produced); and the terminology decision to call the FX numbering "faithful" (structure-preserving), explicitly adopted as this project's own stated design rationale going forward, not as a claim about recovered historical intent (that historical-provenance question remains open and skeptical in `notes/culture/Fuxi-sequence-origins.md`, and must not be conflated with the going-forward design-rationale claim). A later 2026-08-21 session added a related but separate analysis (Observation 10, now written) characterizing the Group Table's 20 groups as the Klein four-group orbit partition under cuogua (complement) and zonggua (reversal) — that part **is** now written up. **Files:** `notes/structure/Group-Table-properties.md` (likely home for the mathematical/structural findings, as further numbered Observations); `notes/culture/Fuxi-sequence-origins.md` (likely home for the historical-rationale framing, extending its existing "Implications for this project" section around line ~192-196). **Solution:** ask the user where this should go (a single location, or split as sketched above with cross-links), then write it up — all underlying data is independently re-derivable from `diagrams/group-table.html` / `diagrams/FX_circle_KW_square.html`'s own rendered/embedded data per this project's verification standard, so nothing here depends on unreliable memory.

## Footer nav links don't reuse an already-open tab in Firefox - 2026-08-20 14:11

- **Named-target footer links (`diagrams/FX_KW_spine.html`) aren't reusing an already-open destination tab in the user's actual Firefox browser, even though the tab was itself opened via the same link.** The three footer buttons — "Yijing Pathways", "FX circle / KW square", "Groups table" — each got a `target="..."` attribute this session (`target="yijing-pathways"`, `target="fx-circle-kw-square"`, `target="groups-table"`) specifically so that clicking one when its destination page is already open in another tab would return to/focus that tab instead of opening a new one, matching the pre-existing convention already used elsewhere in this project (`FX_circle_KW_square.html`'s own `target="fx-kw-spine"` and `target="groups-table"` links). **Problem:** verified working correctly in headless Chromium (`context.pages().length` stayed at 2 after clicking the same link twice — see the session's `test_targets.js` in a now-gone scratchpad, easy to recreate) — but the user, testing live in Firefox, reported the link still opens a brand-new tab on a second click, even after explicitly confirming (via the session's `AskUserQuestion`) that the already-open tab *was* itself opened by clicking that same link earlier — which was expected to be the one case where reuse should reliably work. The user had to leave mid-diagnosis, so this is unconfirmed/unresolved, not yet root-caused. **Files:** `diagrams/FX_KW_spine.html` (the three footer links, this session's new `target=` attributes); `diagrams/FX_circle_KW_square.html` (pre-existing `target="fx-kw-spine"`/`target="groups-table"` links on `spine-link-fx`/`spine-link-kw`/`unify-link` — worth checking whether *this* older pre-existing target-reuse behavior also fails for the user in real Firefox, or whether it's specific to the newly-added links, since that would narrow down whether this is a Firefox quirk with named targets in general vs. something specific to this session's change). **Solution:** not yet found — next session should first try to get a precise, minimal repro (e.g. a two-link toy HTML file) to isolate whether this is a Firefox-specific bug/setting around named-window-target matching (there's a real, documented Firefox preference `browser.link.open_newwindow` and related tab-vs-window handling that can affect this), whether it's specific to `file://` origins (already established this session that Firefox isolates each `file://` URL as its own origin, which broke other cross-tab approaches — worth checking if that isolation also affects named-target matching, not just storage/messaging APIs), or whether the *existing* `fx-kw-spine`/`groups-table` targets on `FX_circle_KW_square.html` have the exact same problem and it's simply never been noticed before. Ask the user to check Firefox's `about:preferences#general` tab-opening settings, and/or test target-reuse with a trivial two-page reproduction outside this project's actual files to isolate the variable.

## XianTian Circle Diagrams & Hexagram Group Table - 2026-08-12 13:01

- **Show the logical/systematic design of the XianTian trigram circle** - Make the construction logic of the trigram circle visible to a reader, not just its arrangement. **Problem:** The trigram circle (hero SVG in `index.html`, and the original `diagrams/xiantian-trigram-circle.svg`) currently shows the 8 trigrams correctly positioned and color-coded (self-reversing pair vs. mixed pair via two cross-axes), but a reader can't tell *why* they're arranged this way just by looking — the binary/yin-yang line logic and the self-reversing/mixed grouping aren't explained. **Files:** `index.html` (hero SVG, trigram glyphs now drawn as precise rects), `diagrams/xiantian-trigram-circle.svg`, `diagrams/FuXi21.html`. **Solution:** consider annotating the two cross-axes directly (label what "self-reversing" and "mixed" mean structurally), and/or surfacing the binary line values (111/110/101/100/011/010/001/000) that drive each trigram's position. **UPDATE (2026-08-21 sessions):** substantial work directly relevant to this TODO is now done and written up — it exhaustively verified exactly which trigrams sit where and why (yang tetrad {Zhen,Li,Dui,Qian} = line-1-yang, occupies a clean contiguous 180° semicircle; yin tetrad {Xun,Kan,Gen,Kun} occupies the complementary semicircle — see `notes/structure/Group-Table-properties.md` and the still-unwritten findings 10-16 tracked in the new TODO item above), and separately, Observation 10 in that same file now gives the deepest available answer to "why this arrangement": the whole Group Table's 20 groups are exactly the orbit partition of the Klein four-group generated by cuogua (complement) and zonggua (reversal) acting on the hexagrams — a verified, non-arbitrary construction. None of this has been drawn into `xiantian-trigram-circle.svg` or `index.html`'s hero SVG yet, but the underlying "why" is now fully answerable and should make this TODO much faster to execute.

- **Show how the XianTian trigram circle maps concentrically outward to the XianTian hexagram circle** - Build a new diagram showing the traditional concentric-ring construction: inner ring = 8 trigrams, outer ring = 64 hexagrams. **Problem:** no existing diagram in this project shows this mapping — it's a structural relationship central to the FuXi/XianTian sequence work but currently undocumented visually. **Files:** new diagram, most likely alongside `diagrams/xiantian-trigram-circle.svg` / `diagrams/FuXi21.html`; may draw on `data/spreadsheets/FX-01.ods` for FX hexagram ordering data. **Solution:** reuse the rect-based trigram glyph technique from `index.html`'s hero (font-independent, pixel-precise) for the inner ring; verify hexagram line patterns the same way they were verified for the trigrams (render and read directly, don't recall from memory) before drawing the outer ring.

- **Link the trigram circle and hexagram circle diagrams together** - Once both diagrams above exist, connect them as one coherent piece rather than two independent artifacts. **Problem:** not yet decided whether this means a single combined SVG, or two separate pages/entries cross-linked. **Files:** wherever the two diagrams from the items above end up (likely `diagrams/`), plus the "Structure" strand entries in `index.html`. **Solution:** decide the linking approach once both diagrams are built.

- **Rebuild the hexagram Group Table starting from a spreadsheet** - Redo the Group Table work, this time using a spreadsheet as the canonical starting data source rather than the prior approach. **Problem:** the existing `notes/structure/Group-Table-properties.md` needs to be reworked from scratch with a spreadsheet-first workflow. **Files:** `notes/structure/Group-Table-properties.md`, `data/spreadsheets/FX-01.ods`, `data/spreadsheets/FX-01.xml`. **Solution:** use `data/spreadsheets/FX-01.ods` (or a fresh spreadsheet, if that one isn't the right starting point) as the source of truth for regenerating the group table data and derived properties. **Note:** the 2026-08-21 sessions added substantial new *analytical* content to `Group-Table-properties.md` on top of the existing spreadsheet-independent version — Observations 7-9 (pure/centered/mixed trigram categories, center-to-periphery generation, FX-sum decomposition) and Observation 10 (the Group Table's 20 groups as Klein four-group orbits under cuogua/zonggua), plus the still-unwritten findings 10-16 tracked separately at the top of this file. A future spreadsheet-first rebuild should make sure all of this survives the rebuild or gets re-verified against the new source.

## Trigram Circles, 3D Cubes & Central Taiji Page - 2026-08-13 22:08

- **Build a graphical representation of the FX and KW trigram circles together** - Create a new diagram showing the FuXi (XianTian) trigram circle and the King Wen (HouTian/latter-heaven) trigram circle side by side or otherwise paired, analogous to `diagrams/FX_circle_KW_square.html`'s FX-hexagram-circle/KW-hexagram-square pairing built this session. **Problem:** no diagram in this project currently shows the two trigram orderings (FuXi vs King Wen arrangement of the 8 trigrams) together or in comparison — only the single FuXi trigram circle exists (`diagrams/xiantian-trigram-circle.svg`, and the hero SVG in `index.html`). **Files:** new diagram (likely `diagrams/fx-kw-trigram-circles.html` following this session's naming convention), `diagrams/xiantian-trigram-circle.svg` (existing FX trigram circle to reuse/extend), `index.html` (hero SVG has the established rect-based trigram-glyph rendering technique — reuse it rather than reinventing). **Solution:** the King Wen trigram arrangement (post-heaven/HouTian bagua) is a different, well-known traditional ordering from the FuXi (pre-heaven/XianTian) one used in this project so far — will need its own verified trigram-position data, sourced and cross-checked the same rigorous way the FX↔KW hexagram bijection was (see `notes/structure/FX_to_KW.md`), not assumed from memory.

- **Show how the yin/yang groups between the FX and KW trigram circles relate** - Once both trigram circles exist, add the same kind of group-highlighting/equivalence feature built this session for the hexagram FX circle ↔ KW square (click-to-highlight, matching color, `UNIFY`-style cross-link) — but for the 8 trigrams' yin/yang groupings rather than the 64 hexagrams' quartets/pairs. **Problem:** not yet designed — unclear exactly what "yin/yang groups" means at the trigram level in this project's terms (e.g. pure-yang/pure-yin trigrams vs. mixed ones, or the FuXi self-reversing/mixed cross-axis grouping already noted as unexplained in the pre-existing "Show the logical/systematic design of the XianTian trigram circle" TO-DO above). **Files:** whatever new diagram is built for the item above; `diagrams/FX_circle_KW_square.html` as the interaction-pattern reference (its `showGroup`/`clearGroup`/`HEX_TO_GROUP`/display-box/`UNIFY`-link machinery). **Solution:** resolve the pre-existing open question about the trigram circle's self-reversing-vs-mixed cross-axis grouping first (see the first item in the "XianTian Circle Diagrams & Hexagram Group Table" TO-DO section above), since that's likely the same "yin/yang group" structure this item wants surfaced and compared against the KW trigram ordering. **UPDATE (2026-08-21 sessions):** this is now largely resolved in substance (not yet in any diagram) — see the pure/centered/mixed trigram categories (`Group-Table-properties.md` Observation 7), the separate yang/yin-by-line-1 semicircle split (Zhen/Li/Dui/Qian vs Xun/Kan/Gen/Kun, part of the still-unwritten findings 10-16 tracked at the top of this file), and Observation 10's Klein four-group characterization (cuogua/zonggua orbits) — all directly applicable here.

- **Create 3D cube representations of the trigrams, "à la Z.D. Yung"** - Build a new visual/diagram representing each of the 8 trigrams as a 3D cube. **Problem:** the specific reference ("Z.D. Yung") and exact cube convention intended is not yet clear from this conversation alone — needs clarification from the user before design work starts (e.g. whether it's a known published diagram/book the user has in mind, what axes map to which of the 3 lines, whether yang/yin lines become filled/open cube faces or vertices, etc.). **Files:** new diagram, likely `diagrams/trigram-cubes-3d.html`; may want CSS 3D transforms (`rotate3d`/`perspective`) or an SVG-based pseudo-3D projection consistent with this project's existing SVG-rect line-glyph technique rather than pulling in a 3D library/dependency. **Solution:** ask the user for the source/reference for "Z.D. Yung" and the intended cube convention before implementing, since guessing wrong here would waste a full design-and-build cycle.

- **Build a central-Taiji page linking FX trigrams ↔ FX digrams ↔ Taiji ↔ KW digrams ↔ KW trigrams** - New page with the Taiji (yin-yang) symbol at its center, flanked outward on one side by FX digrams then FX trigrams, and on the other side by KW digrams then KW trigrams. **Problem:** "digrams" (2-line figures, the 4 possible combinations of two stacked yin/yang lines) are a new concept not yet represented anywhere in this project — only trigrams (3-line, 8 combinations) and hexagrams (6-line, 64 combinations) exist so far, so the digram data/ordering (both FX and KW variants) needs to be defined and verified from scratch, the same rigorous way trigram and hexagram data has been throughout this project (derive and cross-check programmatically, don't hand-type). **Files:** new page, likely `diagrams/taiji-trigrams-digrams.html`; `index.html` hero SVG and `diagrams/xiantian-trigram-circle.svg` for the existing rect-based line-glyph rendering technique to extend down to 2-line digrams and up to reused 3-line trigrams. **Solution:** clarify with the user what "FX digrams" vs "KW digrams" actually means (KW's traditional sequence is normally defined at the trigram/hexagram level, not digram level — the FX/KW digram distinction may need to be derived by the user's own logic, not an existing classical source) before building; likely depends on the "yin/yang groups" work above being resolved first, since the whole point of this page is showing the progressive binary construction (Taiji → digrams → trigrams) symmetrically for both orderings.

## KW Square Distance Markers - 2026-08-14 14:15

- **Add counting markers and distance brackets to the KW square** - Give `diagrams/kw-square.html` the same per-group distance annotation that `diagrams/FX_circle_KW_square.html` already has on the FX circle side. **Problem:** `FX_circle_KW_square.html` draws, per selected group, counting markers plus a bracket between paired members showing their distance apart (`buildDistanceLink()` around line 1752, drawing into the `#distance-overlay` `<g>`, populated on `showGroup()` and cleared via `overlay.innerHTML = ''` in `clearGroup()` — see the `.distance-link`/`.distance-tick`/`.distance-label-bg`/`.distance-label-text` CSS around line 126). `kw-square.html` has no equivalent yet. **Files:** `diagrams/kw-square.html` (to build); `diagrams/FX_circle_KW_square.html` (reference implementation, `buildDistanceLink()` and the `showGroup`/`clearGroup` pop-up wiring). **Solution:** port the same show-on-select/clear-on-deselect behavior, but note the KW square is a square grid, not a circle — `buildDistanceLink()`'s arcs (`arcPath()`, `.distance-link` paths) don't apply directly. The KW brackets need to be **straight lines with tick marks** (not arcs) — keep the tick/label-bubble parts of the pattern, replace the arc geometry with line geometry appropriate to the square layout.
</current_todos>

<original_task>
This session opened with the user asking to read `whats-next.md`, the handoff from the prior session (2026-08-21, the "Phase 1/Phase 2" session that built selectable distance badges and Observations 7-9). From there the session had three distinct phases, none of them originally scoped in advance — each was a new direction the user opened as the session progressed:

1. **Commit verification.** The user asked to commit the two files (`diagrams/group-table.html`, `notes/structure/Group-Table-properties.md`) the prior handoff described as uncommitted. On checking, they were already committed (as `c803eea`, apparently between sessions, outside any conversation) — no new commit was needed, confirming the project's recurring "handoff staleness" pattern.

2. **Binary-sequence numbering comparison.** The user asked to compare this project's FX (XianTian) hexagram numbering to the classical numbering that reads each hexagram as a 6-bit binary number (the historical Shao Yong / Fu Xi binary sequence associated with Leibniz). This was explicitly framed by the user afterward as a one-off analytical comparison only — **not** a proposal or candidate for an alternative numbering scheme for the project.

3. **Sub-group-formation brainstorm.** The user then asked to brainstorm alternative ways to form sub-ordinate groups within the FX ordering, as a comparison against their own claim that the project's existing "pair complementary pairs with their inverse pairs" method (i.e. the Group Table's grouping) is a strong/near-optimal way to do this. This was run through the `superpowers:brainstorming` skill (classified as a **spike** — an analytical question, not code to keep), and produced a solid new verified fact: the existing grouping is exactly the orbit partition of the Klein four-group generated by cuogua (complement) and zonggua (reversal). The user then asked to write this up, which became the session's only file-writing task, plus the this-message's explicit follow-on: "update first to-dos and then the whats-next and then 'commit and push'" — i.e. update `TO-DOS.md`, refresh `whats-next.md` (this document), then commit and push both. This handoff is being written as step 2 of that three-step closing instruction; **step 3 (commit and push) has not happened yet** — see `<work_remaining>`.
</original_task>

<work_completed>

## 1. Commit check (no-op, confirms handoff-staleness pattern again)

Ran `git status --short` / `git diff --stat` / `git log --oneline -5` at the very start of the session. Found the working tree already clean and `c803eea` ("Make group-table distance badges selectable, add trigram-category observations") already present in the log — the two files the prior `whats-next.md` described as uncommitted had, in fact, already been committed between sessions. No commit was made (none was needed). This is the same staleness pattern flagged by this project's `feedback_handoff-staleness-check` memory, now confirmed a second time across two consecutive handoffs.

## 2. Binary-sequence numbering comparison (discussion only, not written to any file)

User asked to compare the project's FX numbering to "the numbering which reads the hexagrams as a binary sequence," explicitly directing a web search ("look for 'Leibniz I Ching' or 'binary I Ching'") rather than working from memory.

- **Sourced the classical convention via `WebSearch`/`WebFetch`** (see Sources below): the Shao Yong/Fu Xi binary sequence reads each hexagram's 6 lines as a 6-bit number with **bottom line = least significant bit, yang = 1, yin = 0**, count 0–63 (Kun = 000000 = 0, Qian = 111111 = 63). This matches this project's own existing bit-table convention (line1 first/lowest in every trigram tuple already used throughout the project).
- **Verified this project's own data against an independently sourced KW1–KW64 binary-value table** (found via `WebSearch`, quoted in full in the search result): parsed `HEXLINES` (FX→6-line pattern) and the `data-hex`/`data-kw` mapping directly out of `diagrams/FX_circle_KW_square.html` (not retyped), computed each hexagram's binary value, and cross-referenced by King Wen number against the sourced table. **Result: 0/64 mismatches** — this both confirms the classical convention was reconstructed correctly and independently validates this project's own line data / FX↔KW mapping against an external source (a nice bonus beyond the original ask).
- **Compared FX numbering to binary-sequence rank directly:** computed each FX-hexagram's binary value (`fx_binval`), and its resulting rank if hexagrams were numbered 1–64 by ascending binary value instead (`binseq_rank = fx_binval + 1`, trivially, since it's a straight sort). Findings:
  - **Pearson correlation between FX number and binary-sequence rank ≈ 0** (−0.0007) — the two numberings are statistically unrelated; FX is not a disguised/shifted version of the classical binary sequence.
  - **FX-parity exactly tracks binary-value parity, 64/64** — not a new finding (restates the already-established "FX odd ⟺ line1 yang" fact from the prior session, since line1 is now understood as literally the binary encoding's LSB), but confirms it's the *only* point of contact between the two schemes.
  - **Opposite-hexagram pairs (FX 2k−1/2k) have a clean arithmetic signature under binary numbering too:** `binval + binval(opposite) = 63` always, since inverting every line inverts every bit — the same opposite-pair fact from the prior session, expressed differently (sum-to-constant vs. FX's numeric adjacency).
- **User then explicitly constrained the framing**, immediately after this was presented: *"this as an analytical comparison and is in no way to be considered as alternative numbering for this project."* Confirmed clearly back to the user — FX stays exactly as-is; this comparison is background/context only, never a design candidate.
- **This entire comparison (sourcing, verification-against-external-table, correlation analysis) was NOT written to any project file** — it exists only in this conversation and in this handoff. See `<work_remaining>` for whether/where to preserve it.

## 3. Sub-group-formation brainstorm → Observation 10 (written)

User asked to brainstorm alternative ways to form sub-ordinate groups within the FX ordering, framed as testing their claim that the existing Group Table's "pair complementary pairs with inverse pairs" method is a strong/near-optimal choice, and to suggest genuinely comparable alternatives.

- **Invoked `superpowers:brainstorming`** with a directive prompt summarizing the ask. The skill classified this as a **spike** (a feasibility/analytical question whose output is an answer, not kept code) — announced the classification, proposed a 2-3 sentence probe plan, got the user's "yes" before investigating.
- **Investigation, step 1 — confirmed exactly what generates the existing scheme.** Read `notes/structure/Group-Table-properties.md` in full to recover the documented construction logic (Observations 1, 2, 6 — row-twins via inner/outer-trigram complement, the "three-flip pattern" {1,6}/{2,5}/{3,4}, axis definition via line1 vs line6). Then computed directly from `HEXLINES` (parsed fresh from `diagrams/FX_circle_KW_square.html`, not retyped) that the existing 20 `GROUPS` entries are **exactly** the orbit partition of the **Klein four-group V4 = {id, complement, reversal, complement∘reversal}** acting on all 64 hexagrams, where:
  - **complement (錯卦/cuogua)** = flip every line (yang↔yin) — already known from the prior session to be exactly the FX(2k−1)/FX(2k) opposite-pairing.
  - **reversal (綜卦/zonggua)** = read the hexagram upside-down (line1↔6, 2↔5, 3↔4).
  - Verified with a Python orbit-closure computation: `{comp,rev}`-orbits produce exactly 20 groups (8 of size 2, 12 of size 4), and comparing this partition against the real `GROUPS` array's `members` fields gives an **exact set-equality match, zero discrepancies**.
  - Orbit-size logic: a hexagram's orbit collapses from 4 to 2 exactly when `reversal(x) == complement(x)` for that hexagram — which happens exactly when both its trigrams are self-reversing (pure or centered, per Observation 7) — cleanly explaining why rows 4–7 are pairs and the rest are quartets.
- **Investigation, step 2 — tested the natural "next" candidate: adding trigram-swap as a third generator.** `tswap(bits) = bits[3:6]+bits[0:3]` (swap inner/outer trigram) is a genuine third involution — confirmed it's an involution (0 failures across all 64), confirmed it's *independent* of {comp,rev} (coincides with one of comp/rev/comp∘rev on only 20/64 hexagrams, not all), and confirmed its 8 fixed points are exactly the "doubled-trigram" hexagrams FX{1,2,19,20,37,38,55,56} (Qian/Qian, Kun/Kun, etc.). Computed the orbit partition of the group generated by all three operations: **4 pairs, 6 quartets, 4 octets** (14 groups total, sizes sum to 64). Critically, checked whether these orbits respect Observation 6's axis split (line1==line6 vs line1≠line6): **all four octets mix axis A and axis B** — trigram-swap moves line1's role to where line4 sat, so it structurally cannot preserve that boundary. This means the richer group buys size at the direct cost of the invariant Observations 2, 3, 8, 9 all depend on — a genuine trade-off, not a strict improvement.
- **Investigation, step 3 — qualitative assessment of other classical/combinatorial candidates** (not deeply recomputed, reasoned from already-established facts): complement-alone (strict subset of existing scheme, can't produce quartets or center/periphery laws); reversal-alone (close to King Wen's own adjacency principle — and the prior session's King-Wen control test already showed KW numbering fails FX's exact-sum laws at chance level, suggestive but not conclusive since that tested numbering not grouping); trigram-swap-alone (uneven 28-pairs+8-singletons partition, less clean than the existing uniform 2s/4s split); Jing Fang's Eight Palaces (八宮, real major historical alternative, but generative/directional line-change construction, not symmetric-involution-based — a different species); yang-line-count/Hamming-weight bands (sizes 1,6,15,20,15,6,1, Pascal's-triangle-shaped, real complement-driven duality but a different combinatorial family, uneven sizes).
- **Presented findings and a verdict** to the user: among symmetric, axis-respecting, involution-based groupings, the existing cuogua/zonggua scheme is at or very near the natural optimum — every real alternative checked either sits strictly inside it, buys size at the cost of axis-purity, or belongs to a genuinely different combinatorial family.
- **User asked to write this up, specifically flagging the Klein-four-group characterization as "a solid, verified new fact."** Wrote it into `notes/structure/Group-Table-properties.md` as new **Observation 10** ("Group construction as Klein four-group orbits (cuogua/zonggua)"), covering: the cuogua/zonggua definitions, the exact-match verification against `GROUPS`, the orbit-size/pair-vs-quartet explanation, a "why this matters for optimality" note, and a condensed version of the candidate-comparison table (trigram-swap extension with its verified 4/6/4 orbit sizes and axis-mixing caveat; the four other qualitative candidates; and a closing verdict paragraph). Updated the file's "Overview" numbered list (line ~55) to add item 10. **Did not** write up the binary-sequence comparison (item 2 above) into this file or any other — the user's "write this up" request was specifically about the Klein-four-group brainstorm, not the earlier binary-numbering discussion.
- **Verified heading structure parses cleanly** after the edit via `grep -n "^#\|^##\|^###"` — confirmed `### 10.` sits correctly under `## Observations`, with `## Background`/`## Diagram` intact below it (same verification method used in the prior session for Observations 7-9).

## 4. TO-DOS.md updated (this message's request, step 1 of 3)

Read the actual `TO-DOS.md` on disk (confirming its prior contents matched what the previous `whats-next.md`'s `<current_todos>` had reproduced) and made four edits:

- **Added a new top section**, "Write up Group-Table-properties.md findings 10–16 from the 2026-08-21 session - 2026-08-21", converting the prior handoff's "highest priority, not yet decided where" work-remaining item into a persistent, explicit TODO so it isn't lost across further sessions (it had been living only in handoff documents until now). Full text is in `<current_todos>` above.
- **Added an "UPDATE (2026-08-21 sessions)" annotation** to the "Show the logical/systematic design of the XianTian trigram circle" item (under "XianTian Circle Diagrams & Hexagram Group Table"), propagating the prior handoff's draft UPDATE text (which previously existed only in `whats-next.md`, not in the real file) and extending it to also mention Observation 10.
- **Added a "Note"** to the "Rebuild the hexagram Group Table starting from a spreadsheet" item, extending the prior handoff's draft note to also cover Observation 10 and the still-unwritten findings 10-16.
- **Added an "UPDATE (2026-08-21 sessions)" annotation** to the "Show how the yin/yang groups between the FX and KW trigram circles relate" item, propagating the prior handoff's draft UPDATE text and extending it to mention Observation 10.

All four edits are now live in the actual `TO-DOS.md` on disk (previously some of this existed only inside `whats-next.md`'s `<current_todos>` reproduction, per that prior handoff's own note that it hadn't been written back to the real file yet).

## 5. This document (`whats-next.md`) refreshed (this message's request, step 2 of 3)

This file itself — written now, per the user's explicit three-step instruction ("update first to-dos and then the whats-next and then 'commit and push'"). Step 3 (commit and push) is next.

</work_completed>

<work_remaining>

## Immediate: step 3 of the user's three-step instruction — commit and push

The user's instruction this message was explicitly: **"update first to-dos and then the whats-next and then 'commit and push'."** Steps 1 (`TO-DOS.md`) and 2 (this `whats-next.md`) are done. **Step 3 — commit and push — has not been done yet** and is the immediate next action for whoever/whatever resumes (very possibly the same session, if this handoff was written mid-flow rather than at a hard context break — check the conversation before assuming a fresh session is needed). Commit should cover:
- `TO-DOS.md` (four edits, this session)
- `notes/structure/Group-Table-properties.md` (Observation 10, this session)
- `whats-next.md` (this refresh, this session — decide whether the user wants this file committed too; prior sessions' handoffs don't show a consistent pattern of whether `whats-next.md` itself gets committed, worth a quick check of `git log -- whats-next.md` if unsure)

Then push to `origin/master` per this project's established `feedback_push-reminder`/`project_github-setup` pattern (public repo, GitHub Pages auto-deploys from `master`).

## Also still open

- **Findings 10–16 from the prior (first) 2026-08-21 session remain unwritten** — now tracked explicitly as its own TODO item (see `<current_todos>` top item), but the actual writing has not happened. Ask the user where (single location in `Group-Table-properties.md`, split with `Fuxi-sequence-origins.md`, or elsewhere) or use judgement, then write it — full detail of exactly what these findings are is preserved in that TODO item's text and was originally documented at length in the prior `whats-next.md` (findings 10 through 16, plus the "faithful" terminology decision and the King-Wen control test).
- **The binary-sequence numbering comparison (this session's item 2) is not written up anywhere.** It's fully reproducible (all data pulled fresh from `diagrams/FX_circle_KW_square.html`, sources cited below), but currently exists only in this conversation. Not flagged as urgent by the user, but worth asking whether it should be preserved anywhere (e.g. a short note in `Fuxi-sequence-origins.md` alongside the historical-provenance discussion, given the strong instruction that it must be framed as analytical-only, never as an alternative-numbering proposal) or intentionally left undocumented.
- **All of `<current_todos>` above remains open** except for the annotation updates made this session — the Firefox tab-reuse bug is still the longest-standing, oldest unresolved item, and none of the diagram-building TODOs (trigram circle annotations, concentric hexagram circle, FX/KW trigram-circle pairing, 3D cubes, Taiji/digram page, KW square distance markers) have been started.
- Consider whether the Klein-four-group / cuogua-zonggua characterization (Observation 10) should also feed into the **"Show how the yin/yang groups between the FX and KW trigram circles relate"** TODO and the **trigram circle logic** TODO more concretely than the current text-only UPDATE annotations — i.e. whether it's time to actually start drawing this into a diagram, now that three separate structural explanations (pure/centered/mixed categories, the yang/yin semicircle split, and the Klein-four-group orbit structure) all converge and are fully verified.

</work_remaining>

<attempted_approaches>

- No dead ends or failed approaches this session — unlike some prior sessions, every investigative approach here worked on the first attempt (the `WebSearch`/`WebFetch` sourcing for the binary-sequence convention succeeded directly; the Python orbit-computation for the Klein-four-group matched the real `GROUPS` data exactly on the first computation, no debugging needed).
- One thing worth flagging as a near-miss rather than a failure: the very first attempt to understand what relates FX3 and FX63 in the existing `GROUPS.pairs` field guessed "reversal" (`bits_to_fx[reversal(fx_bits[3])]` was computed and found to equal 64, not 63 as the `pairs` field's `[3,63]` entry seemed to imply) — this initial guess was wrong, prompting a return to reading `Group-Table-properties.md` directly rather than reverse-engineering the code's `pairs`/`dual`/`axis` fields from scratch. Reading the doc's own prose (Observations 1, 2, 6) turned out to be much faster than guessing at operations and checking them one by one — worth remembering as a general lesson: this project's `notes/structure/*.md` files document *why* the data-structures are shaped as they are, and are often a faster path to understanding than reverse-engineering the live HTML/JS alone.

</attempted_approaches>

<critical_context>

## Sources consulted for the binary-sequence comparison (item 2)

- [Yijing Dao — Shao Yong square, Xiantian diagram sequences](https://www.biroco.com/yijing/sequence.htm)
- [Scripting the Shao Yong Diagram](https://www.russellcottrell.com/blog/ShaoYongScript.htm)
- [Hexagram Orderings Explained — King Wen, Mawangdui, Eight Palaces, and Binary](https://iching.rocks/learn/hexagram-orderings) (this page's search-result excerpt supplied the full sourced KW1–KW64 binary-value table used for the 0/64-mismatch verification)
- [Leibniz and the I Ching: How the 64 Hexagrams Became Binary Code](https://ichingai.info/en/learn/leibniz-i-ching/)

## Explicit user constraint on the binary-sequence comparison — must not be relaxed without the user re-opening it

The user was very deliberate about this: *"this as an analytical comparison and is in no way to be considered as alternative numbering for this project."* Any future write-up of this material (if the user decides to preserve it — see `<work_remaining>`) must keep this framing explicit and up front, not just implicitly obvious from context. Do not let this drift into "here's a numbering option" language even inadvertently.

## The Klein four-group finding, precisely (Observation 10, `notes/structure/Group-Table-properties.md` line ~388 onward)

- **complement / cuogua**: flip every line. Already established (prior session) as exactly the FX(2k−1)/FX(2k) adjacent-pair relation.
- **reversal / zonggua**: read the hexagram upside-down (reverse line order top-to-bottom).
- **V4 = {id, complement, reversal, complement∘reversal}**, acting on the 64 hexagrams (represented as 6-tuples of bits, index0=line1/bottom, matching this project's established convention throughout).
- Orbits of V4 = exactly the 20 groups in `GROUPS` (`diagrams/FX_circle_KW_square.html`'s embedded JS array) — verified by set-equality comparison after independently computing the orbit closure in Python, zero discrepancies.
- Orbit size 2 (pair-rows 4-7, both axes = 8 groups) exactly when `reversal(x) == complement(x)`, i.e. when both of a hexagram's trigrams are self-reversing (pure or centered). Orbit size 4 (quartet-rows, 12 groups) otherwise.
- **Regeneration method if this needs re-deriving:** parse `HEXLINES` from `diagrams/FX_circle_KW_square.html` (regex `var HEXLINES = (\{.*?\});`, JSON-parse), build `fx_bits[fx] = tuple(1 if l=='yang' else 0 for l in HEXLINES[str(fx)])` (index0=line1), define `complement(b)=tuple(1-x for x in b)`, `reversal(b)=tuple(reversed(b))`, compute orbit closures under `{complement, reversal}`, compare against `GROUPS`'s `members` fields (also parsed from the same file via `var GROUPS = (\[.*?\]);`).

## The trigram-swap extension finding, precisely (also in Observation 10)

- `trigram_swap(b) = b[3:6] + b[0:3]` (swap the upper/lower trigram halves of the 6-bit tuple).
- Verified involution (applying twice returns the original) with zero failures across all 64.
- Its 8 fixed points (hexagrams unchanged by trigram-swap, i.e. inner trigram = outer trigram) are exactly FX{1, 2, 19, 20, 37, 38, 55, 56} — the "doubled-trigram" hexagrams (Qian/Qian, Kun/Kun, Zhen/Zhen, Xun/Xun, Kan/Kan, Li/Li, Gen/Gen, Dui/Dui).
- Adding it as a third generator to {complement, reversal}: orbit closure computed the same way as above but with 3 generators. Result: **4 orbits of size 2, 6 of size 4, 4 of size 8** (14 orbits total, membership sums to 64: 4×2+6×4+4×8=8+24+32=64).
- **Critical finding:** all four size-8 orbits mix Observation 6's axis A (line1==line6) and axis B (line1≠line6) membership — checked directly per-orbit, not assumed. This is because trigram-swap relocates what sits at line1 to where line4 was, which has no reason to preserve the line1-vs-line6 equality/inequality that defines the axis split.

## Relevant persistent memory (see `/home/greg/.claude/projects/-home-greg-pCloudDrive-YIJING-Yijing-Pathways/memory/`)

`feedback_verification-standard` (applied throughout — the Klein-four-group claim, the trigram-swap orbit sizes, and the binary-sequence cross-check were all verified against live-parsed project data, not memory or assumption), `feedback_handoff-staleness-check` (confirmed again this session — see `<work_completed>` item 1), `feedback_whats-next-leads-with-todos` (followed — see `<reading_instructions>`), `feedback_push-reminder` (directly relevant — the user's own step 3 instruction *is* "commit and push", so this memory's guidance is satisfied by direct instruction rather than needing a reminder), `project_github-setup`, `project_asana-removed`, `reference_pcloud-backups`, `reference_obsidian-vault`, `user_extreme-zoom-out-viewing` (not directly invoked this session), `project_spine-page-css-gotcha` (not directly relevant, no spine-page work this session).

</critical_context>

<current_state>

## Git status (as of writing this handoff, before step 3/commit)

```
$ git status --short
 M TO-DOS.md
 M notes/structure/Group-Table-properties.md
```

(`whats-next.md` itself will also show as modified/untracked once this write completes, depending on whether it's already tracked in git — check `git status` again after this file is written, before committing.)

```
$ git diff --stat  (as of just before the TO-DOS.md edits landed)
 TO-DOS.md                                 | 10 +++-
 notes/structure/Group-Table-properties.md | 94 +++++++++++++++++++++++++++++++
 2 files changed, 101 insertions(+), 3 deletions(-)
```

(Diff stat will grow slightly further once the final `TO-DOS.md` edits — the 3rd and 4th UPDATE annotations — are counted; the figure above was captured mid-edit-sequence. Re-run `git diff --stat` fresh before committing rather than trusting this number.)

```
$ git log --oneline -3
c803eea Make group-table distance badges selectable, add trigram-category observations
dfacacb Remove spine-side distance brackets, add KW-distance badges to group table
d62f08c Add hexagram display boxes, click-to-select, and button footer nav to FX/KW spine page
```

## Deliverable status

- **`notes/structure/Group-Table-properties.md`**: Observations 1–9 unchanged (pre-existing, from the prior 2026-08-21 session); **Observation 10 newly written this session** (Klein four-group / cuogua-zonggua characterization + candidate comparison + verdict). Overview list updated to include item 10. Heading structure verified to parse cleanly. **Uncommitted.**
- **`TO-DOS.md`**: four edits this session (one new top section, three UPDATE/Note annotations propagated from the prior handoff's draft text). **Uncommitted.**
- **`whats-next.md`** (this file): freshly written this session per explicit user instruction (step 2 of 3). Status re: git tracking/commit intent not yet confirmed — check `git status` and ask the user if unsure whether they want it committed alongside the other two files.
- **`diagrams/group-table.html`, `diagrams/FX_circle_KW_square.html`, and all other project files**: untouched this session (read-only reference/data source for the brainstorm and comparison work).
- **Binary-sequence comparison and findings-10-16 write-ups**: both remain undocumented in any project file — see `<work_remaining>`.

## Immediate next step for whoever/whatever resumes

1. If resuming as a fresh session: greet/resume with the user, lead with `<current_todos>` as instructed.
2. **Run step 3 of the user's instruction: commit `TO-DOS.md` and `notes/structure/Group-Table-properties.md` (and likely `whats-next.md`), then push to `origin/master`.** This is a direct, already-given user instruction from this same conversation turn — should not require re-confirming with the user unless something looks off (e.g. unexpected additional diffs, `git status` showing files this handoff doesn't account for).
3. After push, everything in this session's own scope is complete; remaining work is entirely the pre-existing backlog in `<current_todos>` / `<work_remaining>`.

</current_state>

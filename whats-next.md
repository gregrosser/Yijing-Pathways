<reading_instructions>
Whoever reads this document — a fresh Claude instance resuming this work, at the start of a new session — must lead their first response to the user with the contents of <current_todos> below, before summarizing <original_task>, <work_completed>, or anything else in this document.
</reading_instructions>

<current_todos>
# TO-DOS

## Write up FX-circle geometric properties (diametric complements, inner-trigram Gray cycle) from the 2026-08-22 session - 2026-08-22

- **A rigorous, exhaustively-verified investigation of the actual geometric structure of the given FuXi/Xiantian hexagram circle (as drawn in `diagrams/FX_circle_KW_square.html`, not assumed from memory) was produced this session but has not been written into any project file.** This is a different, deeper analysis than Observation 11's binary-*numbering* comparison — it's about the circle's own intrinsic geometry, prompted by the user asking which numbering "fits" three properties they identified as fundamental to the given circle: (1) complementary opposites at diametric positions, (2) an 8×8 trigram-pair grid structure, (3) a line-by-line doubling/block pattern. **Problem — none of the following is written up anywhere:**
  - **Circle geometry was reverse-engineered directly from the SVG** (not assumed): parsed the 8 `<g transform="rotate(...)">` wedge groups and the 64 `<g class="glyph" id="glyph-N">` elements' `<title>` angle text, computed each hexagram's true absolute angular position, and derived the actual walking order around the circle (regeneration method below).
  - **Property 1 (diametric complements) — confirmed exactly, geometrically, for all 32 pairs, zero exceptions:** FX(2k−1) and FX(2k) sit at precisely 180° apart on the circle, for every k=1..32 (verified from raw angle data, not inferred from line values as in earlier sessions — this is the first *geometric* confirmation of the relation Obs. 10/11 established from line data alone).
  - **Property 2 (trigram grid) — refined:** the circle's macro structure is 8 sectors of 45° each, organized by **inner** trigram (lines 1–3) in the canonical Xiantian circle order already in this file's Background section (`Qian, Xun, Kan, Gen, Kun, Zhen, Li, Dui`). The **outer** trigram's sub-order within each sector is *not* an independent second application of that circle order — only 2 distinct 8-element outer-trigram sequences occur across all 8 sectors, each the exact reverse of the other, and the sub-order is inherited from the pre-existing FX numbering rather than being a free geometric choice.
  - **Property 3 (line-doubling pattern) — the major finding: the inner trigram (lines 1–3) forms an exact 3-bit *reflected Gray code* cycle, not plain binary counting.** Walking the circle counter-clockwise and aligning to the line-1 boundary: line 1 splits into two clean 32-blocks (yang/yin); within the yang half, line 2 orders as (yang×16, yin×16); within the yin half, line 2 orders as the exact *mirror* (yin×16, yang×16) — confirmed to be a true reflection (the whole yin-half sequence for lines 2–3 is the reverse-order mirror of the yang-half sequence, not an independent pattern). This extends cleanly through **line 3** as well (a full inner-trigram Gray cycle — the "6 blocks not 8" originally reported for line 3 is not an anomaly, it's the expected seam artifact where a true Gray *cycle*'s last and first codewords differ by one bit too). **It does not extend to lines 4–6 (outer trigram):** direct Hamming-distance measurement between every pair of geometrically-adjacent hexagrams around the full 64-circle gives distance 1 for only 34/64 steps, with the rest distributed 16 at distance 2, 8 at distance 3, 4 at distance 4, 2 at distance 5 (zero at distance 6) — a strikingly clean halving distribution, but not a pure single-bit-flip Gray cycle overall.
  - **Important terminology/scope flag for any future write-up:** Gray code and plain positional binary value are two different numbering schemes, not the same thing under a different name — this session's Gray-cycle finding is distinct from Observation 11's binary-*rank* comparison. Also flag the bit-order discrepancy noticed along the way: Observation 11's binary convention (line 1 = least-significant bit) was externally sourced and validated against a published KW1–64 table; the *opposite* convention (line 1 = most-significant bit) is the one that reproduces the classical "line1=32-block,...,line6=alternating" cascade description, and has **not** been externally cross-validated the way the LSB convention was — don't conflate the two as interchangeable.
  - **Explicitly out of scope, per the user's direct instruction closing this session:** do **not** compute how a full 6-bit Gray-code numbering (extending the reflection through lines 4–6) would compare to FX numbering on the direct-complement or trigram-clustering measures already established in Observation 11. This was offered and explicitly declined — don't re-open it without the user asking again.
  - **Regeneration method:** parse `diagrams/FX_circle_KW_square.html` for `<g transform="rotate\((-?[\d.]+) 500 500\)">` (8 wedge rotations) and `<g class="glyph" id="glyph-(\d+)">\s*<title>(\S+)\s+(-?[\d.]+)°</title>` (each hexagram's local angle within its wedge); absolute angle = local angle + enclosing wedge's rotation, mod 360; sort by angle (increasing = counter-clockwise, per the user's stated reading direction) to get the true circle walking order; cross-reference against `HEXLINES` (same file) for line values. Outer-trigram names come from `<g class="glyph outer" id="glyph-outer-(\d+)">\s*<title>outer (\S+) \(over \d+\)</title>`.
- **Files:** `notes/structure/Group-Table-properties.md` is the most likely home (extends naturally from Observation 11, or as a new Observation 12), but ask the user first — this session didn't reach a decision on where it goes, only that it isn't written yet.

## Write up Group-Table-properties.md findings 10–16 from the 2026-08-21 session - 2026-08-21

- **A substantial block of exhaustively-verified structural findings about the FX Group Table and FX numbering scheme was produced across two sessions (2026-08-21) but still has not been written into any project file.** **Problem:** the first 2026-08-21 session (structural brainstorming on `notes/structure/Group-Table-properties.md`) established, computationally, and left undocumented: the exact-opposite-pair law for all 32 FX pairs (every FX(2k-1)/FX(2k) pair is a full line-by-line inversion); the precise "yang hexagram = line 1 is yang" criterion (zero exceptions, vs. the "majority yang lines" framing which was tested and explicitly rejected — 10 ties + 5 reversals out of 32); the fact that every one of the table's 20 groups is composed entirely of whole opposite-pairs, which explains three of the table's other properties at once; a King-Wen-numbering control test proving the FX/table relationship is not generic to any numbering (FX passes all three tests exactly, KW fails at/near chance level — the single most load-bearing piece of evidence produced); and the terminology decision to call the FX numbering "faithful" (structure-preserving), explicitly adopted as this project's own stated design rationale going forward, not as a claim about recovered historical intent (that historical-provenance question remains open and skeptical in `notes/culture/Fuxi-sequence-origins.md`, and must not be conflated with the going-forward design-rationale claim). A later 2026-08-21 session added a related but separate analysis (Observation 10, now written) characterizing the Group Table's 20 groups as the Klein four-group orbit partition under cuogua (complement) and zonggua (reversal) — that part **is** now written up. **Files:** `notes/structure/Group-Table-properties.md` (likely home for the mathematical/structural findings, as further numbered Observations); `notes/culture/Fuxi-sequence-origins.md` (likely home for the historical-rationale framing, extending its existing "Implications for this project" section around line ~192-196). **Solution:** ask the user where this should go (a single location, or split as sketched above with cross-links), then write it up — all underlying data is independently re-derivable from `diagrams/group-table.html` / `diagrams/FX_circle_KW_square.html`'s own rendered/embedded data per this project's verification standard, so nothing here depends on unreliable memory.

## Footer nav links don't reuse an already-open tab in Firefox - 2026-08-20 14:11

- **Named-target footer links (`diagrams/FX_KW_spine.html`) aren't reusing an already-open destination tab in the user's actual Firefox browser, even though the tab was itself opened via the same link.** The three footer buttons — "Yijing Pathways", "FX circle / KW square", "Groups table" — each got a `target="..."` attribute this session (`target="yijing-pathways"`, `target="fx-circle-kw-square"`, `target="groups-table"`) specifically so that clicking one when its destination page is already open in another tab would return to/focus that tab instead of opening a new one, matching the pre-existing convention already used elsewhere in this project (`FX_circle_KW_square.html`'s own `target="fx-kw-spine"` and `target="groups-table"` links). **Problem:** verified working correctly in headless Chromium (`context.pages().length` stayed at 2 after clicking the same link twice — see the session's `test_targets.js` in a now-gone scratchpad, easy to recreate) — but the user, testing live in Firefox, reported the link still opens a brand-new tab on a second click, even after explicitly confirming (via the session's `AskUserQuestion`) that the already-open tab *was* itself opened by clicking that same link earlier — which was expected to be the one case where reuse should reliably work. The user had to leave mid-diagnosis, so this is unconfirmed/unresolved, not yet root-caused. **Files:** `diagrams/FX_KW_spine.html` (the three footer links, this session's new `target=` attributes); `diagrams/FX_circle_KW_square.html` (pre-existing `target="fx-kw-spine"`/`target="groups-table"` links on `spine-link-fx`/`spine-link-kw`/`unify-link` — worth checking whether *this* older pre-existing target-reuse behavior also fails for the user in real Firefox, or whether it's specific to the newly-added links, since that would narrow down whether this is a Firefox quirk with named targets in general vs. something specific to this session's change). **Solution:** not yet found — next session should first try to get a precise, minimal repro (e.g. a two-link toy HTML file) to isolate whether this is a Firefox-specific bug/setting around named-window-target matching (there's a real, documented Firefox preference `browser.link.open_newwindow` and related tab-vs-window handling that can affect this), whether it's specific to `file://` origins (already established this session that Firefox isolates each `file://` URL as its own origin, which broke other cross-tab approaches — worth checking if that isolation also affects named-target matching, not just storage/messaging APIs), or whether the *existing* `fx-kw-spine`/`groups-table` targets on `FX_circle_KW_square.html` have the exact same problem and it's simply never been noticed before. Ask the user to check Firefox's `about:preferences#general` tab-opening settings, and/or test target-reuse with a trivial two-page reproduction outside this project's actual files to isolate the variable.

## XianTian Circle Diagrams & Hexagram Group Table - 2026-08-12 13:01

- **Show the logical/systematic design of the XianTian trigram circle** - Make the construction logic of the trigram circle visible to a reader, not just its arrangement. **Problem:** The trigram circle (hero SVG in `index.html`, and the original `diagrams/xiantian-trigram-circle.svg`) currently shows the 8 trigrams correctly positioned and color-coded (self-reversing pair vs. mixed pair via two cross-axes), but a reader can't tell *why* they're arranged this way just by looking — the binary/yin-yang line logic and the self-reversing/mixed grouping aren't explained. **Files:** `index.html` (hero SVG, trigram glyphs now drawn as precise rects), `diagrams/xiantian-trigram-circle.svg`, `diagrams/FuXi21.html`. **Solution:** consider annotating the two cross-axes directly (label what "self-reversing" and "mixed" mean structurally), and/or surfacing the binary line values (111/110/101/100/011/010/001/000) that drive each trigram's position. **UPDATE (2026-08-22):** this now has a much sharper answer, verified geometrically from the circle's own SVG data (see the new top TODO item above) — the 8-trigram circle order is exactly a 3-bit reflected Gray code cycle (adjacent trigrams differ by exactly one line, all the way around including the wrap), not an arbitrary arrangement. That, plus the earlier "yang tetrad {Zhen,Li,Dui,Qian}/yin tetrad {Xun,Kan,Gen,Kun}" semicircle split and Observation 10's Klein four-group orbits, gives three independently-verified, convergent structural explanations ready to annotate into the diagram.

- **Show how the XianTian trigram circle maps concentrically outward to the XianTian hexagram circle** - Build a new diagram showing the traditional concentric-ring construction: inner ring = 8 trigrams, outer ring = 64 hexagrams. **Problem:** no existing diagram in this project shows this mapping — it's a structural relationship central to the FuXi/XianTian sequence work but currently undocumented visually. **Files:** new diagram, most likely alongside `diagrams/xiantian-trigram-circle.svg` / `diagrams/FuXi21.html`; may draw on `data/spreadsheets/FX-01.ods` for FX hexagram ordering data. **Solution:** reuse the rect-based trigram glyph technique from `index.html`'s hero (font-independent, pixel-precise) for the inner ring; verify hexagram line patterns the same way they were verified for the trigrams (render and read directly, don't recall from memory) before drawing the outer ring. The FX-circle geometry work in this session's top TODO item (wedge/glyph parsing from `diagrams/FX_circle_KW_square.html`) is directly reusable groundwork for this.

- **Link the trigram circle and hexagram circle diagrams together** - Once both diagrams above exist, connect them as one coherent piece rather than two independent artifacts. **Problem:** not yet decided whether this means a single combined SVG, or two separate pages/entries cross-linked. **Files:** wherever the two diagrams from the items above end up (likely `diagrams/`), plus the "Structure" strand entries in `index.html`. **Solution:** decide the linking approach once both diagrams are built.

- **Rebuild the hexagram Group Table starting from a spreadsheet** - Redo the Group Table work, this time using a spreadsheet as the canonical starting data source rather than the prior approach. **Problem:** the existing `notes/structure/Group-Table-properties.md` needs to be reworked from scratch with a spreadsheet-first workflow. **Files:** `notes/structure/Group-Table-properties.md`, `data/spreadsheets/FX-01.ods`, `data/spreadsheets/FX-01.xml`. **Solution:** use `data/spreadsheets/FX-01.ods` (or a fresh spreadsheet, if that one isn't the right starting point) as the source of truth for regenerating the group table data and derived properties. **Note:** the 2026-08-21 sessions added substantial new *analytical* content to `Group-Table-properties.md` on top of the existing spreadsheet-independent version — Observations 7-9 (pure/centered/mixed trigram categories, center-to-periphery generation, FX-sum decomposition), Observation 10 (Group Table's 20 groups as Klein four-group orbits under cuogua/zonggua), and Observation 11 (binary-sequence numbering comparison, including this session's added trigram-pair-perspective subsection), plus the still-unwritten findings 10-16 and the 2026-08-22 FX-circle geometry/Gray-cycle findings tracked separately at the top of this file. A future spreadsheet-first rebuild should make sure all of this survives the rebuild or gets re-verified against the new source.

## Trigram Circles, 3D Cubes & Central Taiji Page - 2026-08-13 22:08

- **Build a graphical representation of the FX and KW trigram circles together** - Create a new diagram showing the FuXi (XianTian) trigram circle and the King Wen (HouTian/latter-heaven) trigram circle side by side or otherwise paired, analogous to `diagrams/FX_circle_KW_square.html`'s FX-hexagram-circle/KW-hexagram-square pairing built this session. **Problem:** no diagram in this project currently shows the two trigram orderings (FuXi vs King Wen arrangement of the 8 trigrams) together or in comparison — only the single FuXi trigram circle exists (`diagrams/xiantian-trigram-circle.svg`, and the hero SVG in `index.html`). **Files:** new diagram (likely `diagrams/fx-kw-trigram-circles.html` following this session's naming convention), `diagrams/xiantian-trigram-circle.svg` (existing FX trigram circle to reuse/extend), `index.html` (hero SVG has the established rect-based trigram-glyph rendering technique — reuse it rather than reinventing). **Solution:** the King Wen trigram arrangement (post-heaven/HouTian bagua) is a different, well-known traditional ordering from the FuXi (pre-heaven/XianTian) one used in this project so far — will need its own verified trigram-position data, sourced and cross-checked the same rigorous way the FX↔KW hexagram bijection was (see `notes/structure/FX_to_KW.md`), not assumed from memory.

- **Show how the yin/yang groups between the FX and KW trigram circles relate** - Once both trigram circles exist, add the same kind of group-highlighting/equivalence feature built this session for the hexagram FX circle ↔ KW square (click-to-highlight, matching color, `UNIFY`-style cross-link) — but for the 8 trigrams' yin/yang groupings rather than the 64 hexagrams' quartets/pairs. **Problem:** not yet designed — unclear exactly what "yin/yang groups" means at the trigram level in this project's terms (e.g. pure-yang/pure-yin trigrams vs. mixed ones, or the FuXi self-reversing/mixed cross-axis grouping already noted as unexplained in the pre-existing "Show the logical/systematic design of the XianTian trigram circle" TO-DO above). **Files:** whatever new diagram is built for the item above; `diagrams/FX_circle_KW_square.html` as the interaction-pattern reference (its `showGroup`/`clearGroup`/`HEX_TO_GROUP`/display-box/`UNIFY`-link machinery). **Solution:** resolve the pre-existing open question about the trigram circle's self-reversing-vs-mixed cross-axis grouping first (see the first item in the "XianTian Circle Diagrams & Hexagram Group Table" TO-DO section above), since that's likely the same "yin/yang group" structure this item wants surfaced and compared against the KW trigram ordering. **UPDATE (2026-08-21/22 sessions):** this is now largely resolved in substance (not yet in any diagram) — see the pure/centered/mixed trigram categories (`Group-Table-properties.md` Observation 7), the separate yang/yin-by-line-1 semicircle split (Zhen/Li/Dui/Qian vs Xun/Kan/Gen/Kun, part of the still-unwritten findings 10-16 tracked at the top of this file), Observation 10's Klein four-group characterization (cuogua/zonggua orbits), and this session's verified 3-bit reflected Gray cycle finding for the trigram circle's own order — all directly applicable here.

- **Create 3D cube representations of the trigrams, "à la Z.D. Yung"** - Build a new visual/diagram representing each of the 8 trigrams as a 3D cube. **Problem:** the specific reference ("Z.D. Yung") and exact cube convention intended is not yet clear from this conversation alone — needs clarification from the user before design work starts (e.g. whether it's a known published diagram/book the user has in mind, what axes map to which of the 3 lines, whether yang/yin lines become filled/open cube faces or vertices, etc.). **Files:** new diagram, likely `diagrams/trigram-cubes-3d.html`; may want CSS 3D transforms (`rotate3d`/`perspective`) or an SVG-based pseudo-3D projection consistent with this project's existing SVG-rect line-glyph technique rather than pulling in a 3D library/dependency. **Solution:** ask the user for the source/reference for "Z.D. Yung" and the intended cube convention before implementing, since guessing wrong here would waste a full design-and-build cycle.

- **Build a central-Taiji page linking FX trigrams ↔ FX digrams ↔ Taiji ↔ KW digrams ↔ KW trigrams** - New page with the Taiji (yin-yang) symbol at its center, flanked outward on one side by FX digrams then FX trigrams, and on the other side by KW digrams then KW trigrams. **Problem:** "digrams" (2-line figures, the 4 possible combinations of two stacked yin/yang lines) are a new concept not yet represented anywhere in this project — only trigrams (3-line, 8 combinations) and hexagrams (6-line, 64 combinations) exist so far, so the digram data/ordering (both FX and KW variants) needs to be defined and verified from scratch, the same rigorous way trigram and hexagram data has been throughout this project (derive and cross-check programmatically, don't hand-type). **Files:** new page, likely `diagrams/taiji-trigrams-digrams.html`; `index.html` hero SVG and `diagrams/xiantian-trigram-circle.svg` for the existing rect-based line-glyph rendering technique to extend down to 2-line digrams and up to reused 3-line trigrams. **Solution:** clarify with the user what "FX digrams" vs "KW digrams" actually means (KW's traditional sequence is normally defined at the trigram/hexagram level, not digram level — the FX/KW digram distinction may need to be derived by the user's own logic, not an existing classical source) before building; likely depends on the "yin/yang groups" work above being resolved first, since the whole point of this page is showing the progressive binary construction (Taiji → digrams → trigrams) symmetrically for both orderings.

## KW Square Distance Markers - 2026-08-14 14:15

- **Add counting markers and distance brackets to the KW square** - Give `diagrams/kw-square.html` the same per-group distance annotation that `diagrams/FX_circle_KW_square.html` already has on the FX circle side. **Problem:** `FX_circle_KW_square.html` draws, per selected group, counting markers plus a bracket between paired members showing their distance apart (`buildDistanceLink()` around line 1752, drawing into the `#distance-overlay` `<g>`, populated on `showGroup()` and cleared via `overlay.innerHTML = ''` in `clearGroup()` — see the `.distance-link`/`.distance-tick`/`.distance-label-bg`/`.distance-label-text` CSS around line 126). `kw-square.html` has no equivalent yet. **Files:** `diagrams/kw-square.html` (to build); `diagrams/FX_circle_KW_square.html` (reference implementation, `buildDistanceLink()` and the `showGroup`/`clearGroup` pop-up wiring). **Solution:** port the same show-on-select/clear-on-deselect behavior, but note the KW square is a square grid, not a circle — `buildDistanceLink()`'s arcs (`arcPath()`, `.distance-link` paths) don't apply directly. The KW brackets need to be **straight lines with tick marks** (not arcs) — keep the tick/label-bubble parts of the pattern, replace the arc geometry with line geometry appropriate to the square layout.
</current_todos>

<original_task>
This session opened with the user asking to read `whats-next.md` (the prior 2026-08-22 session's handoff, which had already confirmed a clean/pushed git state and closed out three phases of work — commit verification, a binary-sequence numbering comparison, and a Klein-four-group brainstorm written as Observation 10). From there this session had two connected phases, both explicitly framed by the user throughout as **analysis and comparison only — never a proposal to change the project's FX numbering**:

1. **Trigram-pair perspective on the FX-vs-binary comparison.** The user asked to extend the existing binary-sequence-numbering comparison (Observation 11) by considering trigram pairs specifically. Run as a `superpowers:brainstorming` spike (got a nod on the probe plan first), this produced a genuinely new, exhaustively-verified finding: binary rank is exactly optimal for outer-trigram clustering (the theoretical floor) and weak for inner-trigram clustering, while FX numbering is near-optimal (2× the floor) for inner-trigram clustering and weak (but fully deterministic, not random) for outer-trigram clustering — a clean split rather than either numbering "winning." **This was written to the file** — see `<work_completed>` below.

2. **Direct geometric investigation of the given FX circle itself.** The user redirected the conversation from "propose alternative numberings" (explicitly declined) to "characterize the given circle's own intrinsic structure and see which numbering fits it," naming three properties they considered fundamental: diametric complementary opposites, an 8×8 trigram-pair grid, and a line-by-line doubling pattern. This required reverse-engineering the actual SVG geometry of `diagrams/FX_circle_KW_square.html` (wedge rotations + per-glyph angles) rather than reasoning abstractly, and produced a substantial series of verified findings — most importantly that the circle's inner-trigram structure is an exact 3-bit reflected Gray code cycle. The user then refined their own description of the line-doubling property through several iterations (ending in a precise "digram" nested left/right formula), which was tested and confirmed exactly against the real data. **None of this phase-2 work was written to any file** — see `<work_completed>` and the new top TODO item above for full detail. The user then explicitly declined a further extension (checking a full 6-bit Gray-code numbering against FX on the Observation-11 measures) and asked to close the session: update TO-DOS, refresh this handoff, then commit and push — the three-step closing instruction this document is being written under (steps 1–2 done; step 3 is next).
</original_task>

<work_completed>

## 1. Trigram-pair perspective spike → written as a new subsection of Observation 11

- User asked whether binary numbering might be a better fit than FX numbering when the sequence is interpreted primarily through trigram pairs, given the project's own trigram categories (Obs. 7's pure/centered/mixed) and the already-established binary place-value decomposition (`binary_rank = inner_trigram_value + 8 × outer_trigram_value + 1`, a trivial consequence of the 6-bit encoding's bit positions).
- Ran through `superpowers:brainstorming` as a **spike** (classified out loud, got a nod on a 2–3 sentence probe plan before investigating) — the probe: measure how tightly each numbering clusters the 8 hexagrams sharing a common outer trigram, and separately the 8 sharing a common inner trigram (16 octets total), using range and total pairwise distance, exhaustively (not sampled).
- **Result, verified directly from `HEXLINES`** (parsed fresh from `diagrams/FX_circle_KW_square.html`, not retyped): binary rank is *exactly* optimal (hits the theoretical floor, range 7) for outer-trigram octets and weak (range 56, step-8 spread) for inner-trigram octets. FX numbering is *near*-optimal (range 14, double the floor, a clean step-2 arithmetic progression inside one 16-number block) for inner-trigram octets, and weak-but-fully-deterministic (not random — each outer-trigram value occupies a fixed symmetric position-pair `{p, 17−p}` repeated identically in all four 16-blocks) for outer-trigram octets.
- **Verdict:** no single "better fit for trigram pairs" — each numbering is locality-optimized for the *opposite* trigram slot from the other, and which one "wins" depends entirely on which slot (inner/outer) the reader treats as primary.
- **Written to `notes/structure/Group-Table-properties.md`** as a new subsection "Trigram-pair perspective: each numbering optimizes a different trigram slot (2026-08-22)" under Observation 11, between the existing "Inverse-complementary pairs" and "Scope of the 'better fit' claim" subsections. The "Scope" paragraph and the "Regeneration method" note were both updated to reference the new subsection. Heading structure re-verified via `grep -n "^#\|^##\|^###\|^####"` after the edit — parses cleanly, `### 11.` and all its `####` subsections sit correctly. **This edit is currently uncommitted** — see `<work_remaining>`.

## 2. Direct geometric investigation of the given FX circle — entirely unwritten, full detail here and in the new top TODO item

User explicitly reframed the goal: not proposing a new numbering, but understanding the *given* circle's own structure — naming three properties they considered fundamental (diametric opposites, 8×8 trigram grid, line-doubling pattern) — and asking which existing numbering (current FX vs. classical binary) best "fits" that structure.

- **Reverse-engineered the true circle geometry from the SVG**, not assumed: parsed `diagrams/FX_circle_KW_square.html`'s 8 `<g transform="rotate(...)">` wedge groups and all 64 `<g class="glyph" id="glyph-N"><title>NAME ANGLE°</title>` elements, computed each hexagram's absolute angular position (local angle + enclosing wedge's rotation, mod 360), and derived the true walking order around the circle. Full regeneration method is in the new top TODO item above and should be treated as authoritative for reproducing any of this.
- **Property 1 (diametric complements) — confirmed exactly and geometrically for all 32 pairs, zero exceptions**: FX(2k−1)/FX(2k) sit at precisely 180° apart, verified from raw angle data (a first — earlier sessions only confirmed the complement relation from line data, not from physical circle position).
- **Property 2 (8×8 trigram grid) — confirmed but asymmetric**: the circle's 8 macro-sectors (45° each) are organized by **inner** trigram in the canonical circle order (`Qian, Xun, Kan, Gen, Kun, Zhen, Li, Dui`, matching the file's existing Background section exactly). The outer trigram's sub-order within each sector is *not* an independent second circular pass — only 2 distinct 8-element sequences occur across all 8 sectors (each the exact reverse of the other), inherited from the pre-existing FX numbering rather than from independent geometry.
- **Property 3 (line-doubling pattern) — refined through several user iterations, then confirmed as a 3-bit reflected Gray code, not plain binary counting.** The user's final precise formulation (a nested "digram" left/right formula: within the line-1-yang half, line 2 orders yang-then-yin; within the line-1-yin half, line 2 orders as the exact mirror, yin-then-yang) was tested directly against the real circle data (aligned to the true line-1 boundary, walking counter-clockwise as the user specified) and **confirmed exactly, no exceptions**. This extends cleanly one level further, through **line 3** — a full 3-bit reflected Gray cycle for the entire inner trigram, which is exactly why the canonical 8-trigram circle order comes out the way it does (adjacent trigrams differ by exactly one line, all the way around including the wrap point). **It does not extend to lines 4–6** (outer trigram): direct Hamming-distance measurement between every geometrically-adjacent pair around the full 64-circle gives distance 1 for only 34/64 steps (rest: 16 at distance 2, 8 at distance 3, 4 at distance 4, 2 at distance 5, none at distance 6) — clean halving proportions, but not a pure single-bit-flip cycle overall.
- **Important flag for any future write-up:** Gray code and plain positional binary value are genuinely different numbering schemes (not a relabeling of each other) — this session's Gray-cycle finding is distinct from Observation 11's binary-rank comparison and must not be conflated with it. Also: the bit-order convention that reproduces the classical "line1=32-block,...,line6=alternating" cascade (line 1 = most-significant bit) is the *opposite* of Observation 11's already-externally-validated convention (line 1 = least-significant bit, cross-checked 0/64 against a published KW1–64 table) — these are two different numberings under the same casual name "binary," and only one has been externally validated.
- **User then explicitly declined a further extension**: checking how a full 6-bit Gray-code numbering (continuing the reflection through lines 4–6) would compare to FX on the direct-complement/trigram-clustering measures already in Observation 11. This is now out of scope — don't re-run it without the user asking again.
- **None of this phase-2 material has been written to any project file.** It's fully reproducible (regeneration method above/in the TODO item), all sourced fresh from the SVG and `HEXLINES`, nothing here depends on memory.

## 3. TO-DOS.md updated (this message's request, step 1 of 3)

Added a new top section, "Write up FX-circle geometric properties (diametric complements, inner-trigram Gray cycle) from the 2026-08-22 session - 2026-08-22", capturing all of phase-2 above in full (properties 1–3, the Gray-code/binary-convention distinction, the explicit out-of-scope flag, and the regeneration method). Placed above the pre-existing "Write up Group-Table-properties.md findings 10–16" item, which remains untouched and still open. Also added a short "UPDATE (2026-08-22)" annotation to the "Show the logical/systematic design of the XianTian trigram circle" item (XianTian Circle Diagrams section) noting the Gray-cycle finding directly answers that TODO's original question, and extended the existing "UPDATE (2026-08-21/22 sessions)" annotation on "Show how the yin/yang groups between the FX and KW trigram circles relate" to mention it too. All edits are live in the actual `TO-DOS.md` on disk.

## 4. This document (`whats-next.md`) refreshed (this message's request, step 2 of 3)

This file itself — written now, per the user's explicit three-step instruction ("update the to-do list and then refresh the whats-next and then 'push and commit'"). Step 3 (commit and push) is next.

</work_completed>

<work_remaining>

## Immediate: step 3 of the user's three-step instruction — commit and push

Steps 1 (`TO-DOS.md`) and 2 (this `whats-next.md`) are done. **Step 3 — commit and push — has not been done yet.** Commit should cover:
- `notes/structure/Group-Table-properties.md` (the new "Trigram-pair perspective" subsection under Observation 11, this session)
- `TO-DOS.md` (new top TODO item plus two annotation updates, this session)
- `whats-next.md` (this refresh, this session)

Then push to `origin/master` per this project's established `feedback_push-reminder`/`project_github-setup` pattern (public repo, GitHub Pages auto-deploys from `master`). Run `git status`/`git diff --stat` fresh before committing rather than trusting any numbers in this document, per this project's recurring handoff-staleness pattern (`feedback_handoff-staleness-check`).

## Also still open

- **The entire phase-2 geometric/Gray-cycle investigation (Properties 1–3, this session) is unwritten** — now tracked in detail as its own top TODO item, but the actual writing into `notes/structure/Group-Table-properties.md` (or wherever the user decides) hasn't happened. This is a rich, fully-verified, non-obvious set of findings (especially the 3-bit reflected Gray cycle) — worth prioritizing before it's lost to memory decay, even though it isn't urgent by the user's own framing this session.
- **Findings 10–16 from the first 2026-08-21 session remain unwritten** — unchanged from prior handoffs, still tracked in its own TODO item.
- **The binary-sequence numbering comparison from the second 2026-08-21 session was in fact written up** (as Observation 11) — a prior handoff had incorrectly flagged it as still-undocumented; that's now resolved and shouldn't be re-flagged.
- **All of `<current_todos>` above remains open** except for the annotation updates made this session — the Firefox tab-reuse bug is still the longest-standing, oldest unresolved item, and none of the diagram-building TODOs (trigram circle annotations, concentric hexagram circle, FX/KW trigram-circle pairing, 3D cubes, Taiji/digram page, KW square distance markers) have been started. The trigram-circle-annotation TODO in particular now has an unusually strong, ready-to-use answer (the Gray-cycle finding) waiting to be drawn into a diagram.
- **Do not extend the Gray-code investigation to a full 6-bit comparison against FX** unless the user explicitly reopens it — they declined this directly at the end of this session.

</work_remaining>

<attempted_approaches>

- No dead ends this session on the substance — every computational check (the trigram-pair octet clustering, the SVG angle-parsing, the diametric-complement verification, the recursive Gray-cycle alignment) matched cleanly on investigation, no debugging required.
- One useful methodological correction mid-session, worth remembering: an early, cruder test of "does each line show a clean doubling block pattern" (checking blocks per line independently, without aligning to the recursive structure) reported what looked like anomalies for lines 3–6 (e.g. "6 blocks not 8" for line 3). Once the analysis was redone *properly aligned* to the recursive reflected structure (rotating the sequence to start exactly at the line-1 boundary, then checking nested sub-blocks), line 3 turned out to be a clean, anomaly-free 3-bit Gray cycle — the earlier "anomaly" was just an artifact of an arbitrary, non-aligned starting point merging blocks at the wrap seam. Lesson: when testing a hypothesized recursive/nested structure, align the analysis to the structure's own natural boundaries before concluding it's messy — a naive flat block-count can make a perfectly clean nested pattern look broken.
- The user iterated their own description of "Property 3" (the line-doubling pattern) twice before landing on the precise nested left/right digram formula that was ultimately confirmed exactly — worth noting for tone/pacing, not as a failure: the first two framings were good-faith approximations of a real structure the user could see in the diagram but hadn't yet fully formalized, and each iteration got computationally closer to what turned out to be an exact match.

</attempted_approaches>

<critical_context>

## Precise Gray-cycle finding (Property 3), for exact regeneration

- Circle walked **counter-clockwise** (increasing angle, standard math convention — user was explicit this direction is the determinative reading direction), aligned to start at the line-1 yang→yin transition boundary (rotate `ccw_order` so index 0 is the first position after a line-1 value change).
- `L1`: positions 0–31 = yang (constant), 32–63 = yin (constant). Two clean cyclic blocks of 32, zero exceptions.
- `L2` within Left (pos 0–31): yang×16 then yin×16. Within Right (pos 32–63): yin×16 then yang×16 — the exact reverse-order mirror of Left, not an independent pattern. Confirmed by direct sequence comparison (Right's L2+L3 subsequence, read backward, equals Left's L2+L3 subsequence exactly).
- `L3` extends the same reflection cleanly: within each of the 4 quadrants (pos 0–7, 8–15, 16–23, 24–31, 32–39, 40–47, 48–55, 56–63 — 8 octets of 8), each octet is homogeneous in line 3, and the octet-level pattern across all 8 continues the same mirror-reflection rule.
- This is a true 3-bit **reflected Gray code cycle**: a cyclic Hamiltonian path on the 3-bit hypercube where every step (including the wraparound from the last position back to the first) changes exactly one bit. This is *why* the classical 8-trigram circle order (`Qian, Xun, Kan, Gen, Kun, Zhen, Li, Dui`) is what it is — it's the unique-up-to-symmetry reflected Gray cycle on 3 bits.
- Extending to lines 4–6 (outer trigram) breaks this: Hamming distance between all 64 geometrically-consecutive pairs (cyclic) = 1 for 34/64 steps, 2 for 16, 3 for 8, 4 for 4, 5 for 2, 0 at distance 6. Not a full 6-bit Gray cycle.
- **Regeneration:** see the TODO item's regeneration method (top of `<current_todos>`) for the exact regex/parsing steps against `diagrams/FX_circle_KW_square.html`.

## Two distinct "binary" conventions — do not conflate

1. **Line 1 = least-significant bit** (Observation 11's convention): externally sourced (Shao Yong/Fu Xi binary sequence, associated with Leibniz) and validated 0/64 against a published KW1–64 binary-value table. This is the convention behind all of Observation 11's direct-complement and trigram-pair-perspective findings.
2. **Line 1 = most-significant bit**: the convention that reproduces the classical "line1=32-block,...,line6=alternating" generative-doubling cascade description. **Not** externally cross-validated the way convention 1 was — used this session only to confirm it trivially reproduces the cascade by construction (2/4/8/16/32/64 block counts), not tested against any external source.

These are bit-reversals of each other, not the same numbering under two names — any future write-up must keep them clearly distinguished.

## Relevant persistent memory

`feedback_verification-standard` (applied throughout — every claim in this session, including the Gray-cycle finding, was verified against data parsed fresh from the SVG/HEXLINES, never from memory), `feedback_handoff-staleness-check` (worth rechecking again on resume — run `git status` fresh), `feedback_whats-next-leads-with-todos` (followed — see `<reading_instructions>`), `feedback_push-reminder` (satisfied directly by this session's own closing instruction), `project_github-setup`, `project_asana-removed`, `reference_pcloud-backups`, `reference_obsidian-vault`, `user_extreme-zoom-out-viewing` (not directly invoked this session), `project_spine-page-css-gotcha` (not relevant, no spine-page work this session).

</critical_context>

<current_state>

## Git status (as of writing this handoff, before step 3/commit)

```
$ git status --short
 M notes/structure/Group-Table-properties.md
```

(`TO-DOS.md` and `whats-next.md` will also show as modified once this write completes — check `git status` again fresh before committing, don't trust this snapshot.)

```
$ git diff --stat  (Group-Table-properties.md only, as of just before the TO-DOS.md/whats-next.md edits)
 notes/structure/Group-Table-properties.md | 89 ++++++++++++++++++++++++++++-
 1 file changed, 87 insertions(+), 2 deletions(-)
```

```
$ git log --oneline -5
38e3599 Add binary-sequence numbering comparison (Observation 11)
f292219 Add Klein four-group characterization of Group Table (Observation 10)
c803eea Make group-table distance badges selectable, add trigram-category observations
dfacacb Remove spine-side distance brackets, add KW-distance badges to group table
d62f08c Add hexagram display boxes, click-to-select, and button footer nav to FX/KW spine page
```

## Deliverable status

- **`notes/structure/Group-Table-properties.md`**: Observations 1–10 unchanged; Observation 11 (from the prior session) unchanged except for one new subsection this session ("Trigram-pair perspective: each numbering optimizes a different trigram slot") plus small updates to its "Scope" and "Regeneration method" text to reference it. Heading structure re-verified. **Uncommitted.**
- **`TO-DOS.md`**: one new top section this session, plus two small annotation updates to pre-existing items. **Uncommitted** (will show as modified after this write).
- **`whats-next.md`** (this file): freshly written this session per explicit user instruction (step 2 of 3).
- **The entire phase-2 geometric/Gray-cycle investigation**: fully verified, zero written to any project file yet — captured in full in `<work_completed>`/`<critical_context>` above and the new top TODO item, reproducible from the regeneration method, not dependent on this document surviving.
- **`diagrams/FX_circle_KW_square.html`, `diagrams/group-table.html`, and all other project files**: untouched this session (read-only reference/data source throughout).

## Immediate next step for whoever/whatever resumes

1. If resuming as a fresh session: greet/resume with the user, lead with `<current_todos>` as instructed.
2. **Run step 3 of the user's instruction: commit `notes/structure/Group-Table-properties.md`, `TO-DOS.md`, and `whats-next.md`, then push to `origin/master`.** Direct, already-given instruction from this same conversation turn — should not require re-confirming unless `git status` shows something unexpected.
3. After push, everything in this session's own scope is complete; remaining work is the pre-existing backlog in `<current_todos>`/`<work_remaining>`, with the new FX-circle-geometry write-up now the most valuable not-yet-started item given how much fully-verified material is sitting ready for it.

</current_state>

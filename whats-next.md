<original_task>
Session began with `@whats-next.md read this` (resuming from a prior session's handoff about Asana/Figma/PDF-import/`index.html` site-build work). From there the session had no single fixed task — it moved through a long, continuous chain of user-directed threads across one very long working day: (1) create Asana tasks for the 4 items already in `TO-DOS.md`; (2) review `data/spreadsheets/FX-01.ods`; (3) build `diagrams/group-table.html` (new artifact) and iteratively refine it, then add hover/click/Ctrl+click interactivity; (4) build `diagrams/kw-square.html` (new artifact, King Wen 8×8 grid) and refine it; (5) add a click-to-show-group "display box" feature to the pre-existing `diagrams/FuXi21.html` (FX circle), then halve its highlight scope, remove its radial spokes, and resize/reposition it through many precise rounds; (6) at a lunch break and an end-of-day break: add TO-DOs and rewrite `whats-next.md`, twice; (7) **[this final stretch, after the last whats-next.md checkpoint]** port the FX-circle's display-box feature to `kw-square.html`; (8) build a brand-new combined artifact `diagrams/FX_circle_KW_square.html` pairing the FX circle and KW square side-by-side with synchronized click-selection, then iteratively refine its sizing, alignment, colors, and add a cross-linking "UNIFY" button to `group-table.html`; (9) implement a general "click away from the graphs deselects everything" rule across all four interactive pages; (10) add a final TO-DO for follow-on trigram/taiji work and write this handoff.
</original_task>

<work_completed>

**Note:** items 1–13 below were already complete and documented in the previous `whats-next.md` checkpoint (written at end-of-day before this final stretch of work); they're summarized here for continuity. Items 14 onward are new since that checkpoint.

**1. Resumed session context** from the prior `whats-next.md`, verified nothing had changed since it was written.

**2. Asana tasks created** for the 4 original `TO-DOS.md` items in the "Yijing Pathways" Asana project (`1217409138148508`), workspace `1217392032513208`, via `mcp__asana__create_tasks`. Gids: `1217451233549723`, `1217445044191799`, `1217445044397001`, `1217451233533825`.

**3. Reviewed `data/spreadsheets/FX-01.ods`** (unzipped, parsed `content.xml` via hand-rolled Python `xml.etree.ElementTree`). Found 3 blocks: Block 1 (FX Group Table, matches `Group-Table-properties.md`), Block 2 (KW cross-reference, used to build `group-table.html`'s KW columns), Block 3 (`n`/`cuo(n)` complement table, still unused).

**4. Built `diagrams/group-table.html`** — new artifact, replaced the old Claude-Artifact-based version linked from `Group-Table-properties.md`. Hexagram line data and group structure extracted programmatically from `FuXi21.html`'s SVG and `GROUPS` array; KW numbers from `FX-01.ods` Block 2. Went through a long iterative formatting pass (ascending FX order, single shared background, set-notation labels `FX{a,b,c,d}`, column reordering, precise gap/alignment tuning) — full history of ~15 individual formatting instructions and 2 rejected over-correction attempts preserved in git-less file history only (no commit log; see `attempted_approaches` below for the two rejections).

**5–6. Added hover/click and Ctrl+click multi-select** to `group-table.html`, reusing the verified FX↔KW bijection from `notes/structure/FX_to_KW.md`. `selected` is a `Set`; plain click replaces selection, Ctrl+click toggles membership additively.

**7. Built `diagrams/kw-square.html`** — new artifact, King Wen sequence as an 8×8 grid, Legge orientation (`KW{1}` top-right, `KW = row*8 - col + 1`). Cell glyphs looked up via `kw_to_fx[KW] → hexlines[FX]`.

**8. Formatted `kw-square.html`**: borderless, tight spacing, fixed the "not square" bug (`flex:none; width:60px` per cell, `.square{width:max-content;margin:0 auto}`).

**9–13. Built the FX-circle display-box feature in `diagrams/FuXi21.html`** (pre-existing page, edited in place): `HEXLINES` data (64-entry, verified byte-for-byte after an initial hand-typed version was found 56/64 wrong), `largeHexSvg`/`pdHexHtml`/`renderPairDisplay`/`clearPairDisplay`, wired into `showGroup`/`clearGroup`. Halved the highlight scope (own group only, not the old 8-hexagon "dual" group — removed `keyFor()`'s dual-merge key, axis-line connector, inner/outer-arc collision logic). Removed the 64 gray radial spoke lines. Four rounds of precise, `getBoundingClientRect()`-verified sizing/positioning: circle +15% (620→713px `.stage` cap), display box −15%/−20%/−10% via `zoom` (0.85→0.68→0.612) with position raised/lowered by exact effective-pixel amounts (compensating for `zoom` scaling `margin` too — see `critical_context`). Fixed a genuine pre-existing bug: the masthead caption contained a literal 6-character `—` escape-sequence instead of a real em-dash.

**14. Ported the display-box feature to `diagrams/kw-square.html`** (start of this final stretch). Resolved the open design question from the TO-DO ("what are 'direct group members' for a KW-indexed grid?") by reusing `FuXi21.html`'s `GROUPS` array + `HEX_TO_GROUP` map: a clicked cell's underlying FX number resolves to its FX group, whose members are then mapped to KW numbers via a `FX_TO_KW`/`KW_TO_FX` bijection built live from the grid's own `data-hex`/`data-kw` attributes (not re-hardcoded). Added `.pair-display`/`.pd-*` CSS (using `kw-square.html`'s own site-design-system palette, added a new `--paper-panel` token), `HEXLINES`/`GROUPS` literals (verified byte-for-byte identical to `FuXi21.html`'s), `largeHexSvg`/`pdHexHtml`/`renderPairDisplay`/`clearPairDisplay`/`showGroup`/`clearGroup`. Caption shows `KW{...}` sorted ascending, split left/right into two slots. Verified via simulated clicks (quartet `FX{3,4,63,64}` → `KW{23,24,43,44}`, pair `FX{1,2}` → `KW{1,2}`, deselect-on-second-click, clean group-switching) and screenshots in both themes.

**15. Built `diagrams/FX_circle_KW_square.html`** — brand-new combined artifact, the FX circle and KW square side by side with **synchronized** click-selection (one shared `currentGroupId`/`GROUPS`/`HEX_TO_GROUP`/`FX_TO_KW` state driving both diagrams at once). Built by extracting the FX-circle stage SVG block and KW-square grid block verbatim from `FuXi21.html`/`kw-square.html` via line-anchored Python slicing (with assertions verifying exact anchor lines before slicing, to avoid the kind of corruption a looser regex approach caused earlier this session), renaming colliding element IDs (`pair-display`→`pair-display-fx`/`-kw`, etc.), and reassembling with the site's `design.md` token palette plus circle-specific tokens (`--gridline`, `--verify` — later removed, see below). `showGroup()` now drives both the FX circle's arc/diametric-line rendering **and** the KW square's `.hl` cell highlighting **and** both display boxes (`renderPairDisplay` populates `pd-caption-fx`/`pd-slot-left-fx`/`-right-fx` and the `-kw` equivalents from the same `GROUPS[i]` in one call). Verified both click directions (clicking the circle drives the square and vice versa) and both light/dark themes.

**16. Iteratively resized/aligned `FX_circle_KW_square.html`** through many precise, measurement-verified rounds:
   - Circle +20% (480→576px `.stage` cap) — user reported not seeing a size change; investigated and confirmed via `getBoundingClientRect()` (576.0px measured) and pixel-bounding-box screenshot analysis (297px→355px, ratio 1.195≈1.2) that the change *was* real and correctly rendered; concluded the discrepancy was most likely browser/pCloud-sync caching on the user's end (their own hard refresh didn't help either — see `critical_context`).
   - "Make circle diameter = KW-square width, center vertically aligned, boxes stay aligned" — first pass set `.stage` container to literally 480px, verified via `getBoundingClientRect()`, but the user still reported the circle looking smaller. Root-caused via `svg.getBBox()`: the circle's SVG content only fills ~87% of its own 1000×1000 viewBox (869.9-unit content bbox out of 1000), so a 480px *container* only rendered a ~417.5px *visible* circle. Fixed by computing the container size needed for the **visible content** (not the container box) to equal 480px: `480 / 0.8699 ≈ 551.8px`. Re-verified: visible content width 480.2px vs square 480.0px. Center-alignment achieved via `align-self:center` on both `.fx-diagram`/`.kw-diagram` grid items (robust to any future height mismatch, not a hardcoded offset).
   - Circle +30% (552→717.6px) and display boxes shrunk 50% (`zoom:0.5`) and raised 100px effective (raw `margin-top:-200px`, since `zoom` scales `margin` by the same factor — `-100/0.5 = -200`). All verified: circle visible content 624.2px (480.2×1.3), box size 406×173→204×88px (≈50%), box raised exactly 100.0px measured against a margin-top:0 baseline copy of the file.
   - Column gap between panels halved (56px→28px `column-gap`), verified via `getBoundingClientRect()` gap measurement = 28.0px exactly.
   - FX-circle highlight color unified to match KW-square's: all `var(--verify)` (green) usages in `.distance-link`/`.distance-tick`/`.distance-label-bg`/`.distance-label-text`/`.diametric-line`/`.glyph.grouped .bar`/`.num-label.grouped text` repointed to `var(--structure)` (the shared red/orange accent); the now-fully-unused `--verify` CSS variable definitions (3 occurrences: base + 2 dark-mode blocks) were removed entirely rather than left dead. **This change is scoped only to `FX_circle_KW_square.html`** — the standalone `FuXi21.html` still uses its own original `--seal`/`--verify` palette, untouched.

**17. Added the "UNIFY" cross-link button** to `FX_circle_KW_square.html`, centered between the two display boxes, linking to `group-table.html?fx=<sorted-fx-list>` (e.g. `?fx=3,4,63,64`), hidden until a group is selected (`.unify-link.active`).
   - First implementation placed it via a 3-column CSS Grid (added a middle column between the FX and KW columns) with `justify-self/align-self:center` — measured this was off by `dx=+59.4px, dy=+72.5px` from the true midpoint between the boxes, because the outer grid columns are sized by the much-wider circle/square content, not by the (smaller) boxes centered within them, so the middle column's own center doesn't coincide with the true gap-midpoint between the actual box edges.
   - Fixed by abandoning grid-based centering for the button: made `.panels` `position:relative`, made `.unify-link` `position:absolute` with `transform:translate(-50%,-50%)`, and added a `positionUnifyLink()` JS function (called at the end of `showGroup()`, and on `window.resize` if a group is active) that computes the true midpoint directly from `getBoundingClientRect()` on both `#pair-display-fx`/`#pair-display-kw` and sets `left`/`top` accordingly. Verified: `dx=-0.01px, dy=0.00px`.
   - **`diagrams/group-table.html` modified** to support this: added `clearAllSelected()` helper (there was no "clear everything" function before, only per-hexagram toggling) and, at the end of its script, parsing of a `?fx=` URL query param on load — splits on commas, calls `toggleSelect(fx, true)` for each member (building the full multi-select highlight via the existing additive-select code path), and scrolls the first matching hexagram into view. Verified: navigating to `group-table.html?fx=3,4,63,64` correctly highlights and bolds `FX{3,4,63,64}`/`KW{23,24,43,44}` and scrolls to that row, in both themes.
   - UNIFY's link target went through 3 iterations: plain link (same-tab navigation, user's first request) → `target="_blank"` (always a new tab) → user clarified they wanted **tab reuse**, not a new tab every time → `target="groups-table"` (named target, standard HTML mechanism for browsers to reuse an existing same-named tab) → user reported it still opened a new tab every time → root-caused via HTML spec/MDN knowledge (not empirically testable in a single headless page): **`rel="noopener"`, which had been added alongside the named target, silently coerces any target other than `_top`/`_self`/`_parent` into `_blank`-like always-new-tab behavior.** Removed `rel="noopener"` (judged safe here since both pages are trusted first-party local files, not an external/untrusted link). Final state: `<a class="unify-link" id="unify-link" href="group-table.html" target="groups-table">`. **This fix is spec-based reasoning, not empirically verified against real multi-tab browser behavior** — worth the user confirming behaviorally (click UNIFY on two different groups, confirm only one Groups Table tab is used).

**18. Implemented "click away from the graphs deselects everything"** as a general rule across all four interactive pages:
   - `diagrams/group-table.html`: `document.addEventListener('click', e => { if (!e.target.closest('.table-wrap')) clearAllSelected(); })`.
   - `diagrams/kw-square.html`: same pattern, checking `.square`/`.pair-display`.
   - `diagrams/FuXi21.html` and `diagrams/FX_circle_KW_square.html`: same pattern, checking `.stage`/`.pair-display` (FuXi21) or `.diagram`/`.pair-display`/`.unify-link` (combined page).
   - **Bug found and fixed**: the FX circle's SVG has a real invisible margin around its visible content (up to ~47px on the combined page, smaller but present on `FuXi21.html` too — same root cause as the "diameter" bug in item 16: ~13% inherent viewBox padding). This margin is still inside the `.stage`/`.diagram` bounding box, so clicks there were being counted as "on the graph" and *not* triggering deselect, even though it looks like blank page. First fix attempt (`pointer-events:none` on just the `<svg>` element) was insufficient — the underlying `.stage` `<div>` (same size) still captured the click and still matched `.closest('.stage')`/`.closest('.diagram')`. Correct fix: `pointer-events:none` on the **whole wrapper** (`.fx-diagram` in the combined page, `.stage` itself in `FuXi21.html`), with `pointer-events:auto` explicitly re-enabled on `.num-label` (the actual clickable elements) to opt back in. Verified on both pages: clicking in the former dead-zone now correctly deselects (`afterDeadZoneClick=false`), and clicking a hexagram number still works normally (`labelStillClickable=true`). `kw-square.html`/`group-table.html` never had this problem (confirmed via corner-click test) since their clickable areas are plain DOM elements sized exactly to their visible content, no inflated/padded container.

**19. Added a new TO-DOS.md section** (`## Trigram Circles, 3D Cubes & Central Taiji Page - 2026-08-13 22:08`) with 4 items — full text in `TO-DOS.md`, summarized in `work_remaining` below.

**20. This `whats-next.md` rewrite** — full replacement of the prior end-of-day checkpoint.

</work_completed>

<work_remaining>

1. **Clean up two now-stale TO-DOS.md entries.** The sections `## Combined FX Circle + KW Square Artifact - 2026-08-13 12:00` and `## Bring KW Square Up to Parity with FX Circle - 2026-08-13 17:15` are both now **fully completed** (by `diagrams/FX_circle_KW_square.html` and item 14 above, respectively) but still sit in `TO-DOS.md` as if open. Remove both sections at the start of the next session.

2. **New TO-DO items from this session's final request** (`TO-DOS.md`'s newest section, full Problem/Files/Solution detail there):
   - Build a graphical representation of the FX **and** KW trigram circles together (paired, analogous to `FX_circle_KW_square.html`'s hexagram pairing) — needs the King Wen (HouTian/post-heaven) trigram arrangement's position data sourced and verified from scratch (only the FuXi/XianTian trigram circle exists currently, in `diagrams/xiantian-trigram-circle.svg` and `index.html`'s hero SVG).
   - Show how the yin/yang groups between the two trigram circles relate — depends on first resolving the *pre-existing* open question (from the original "XianTian Circle Diagrams" TO-DO, still unresolved) about what the FuXi trigram circle's self-reversing-vs-mixed cross-axis grouping actually represents structurally.
   - Create 3D cube representations of the trigrams "à la Z.D. Yung" — **the reference is not understood yet; ask the user what "Z.D. Yung" refers to and what cube convention is intended before designing anything.**
   - Build a central-Taiji page: `FX trigrams ↔ FX digrams ↔ Taiji ↔ KW digrams ↔ KW trigrams` — **"digrams" (2-line figures) are a brand-new concept for this project, not yet defined for either FX or KW ordering; clarify with the user what "FX digrams" vs "KW digrams" should mean before building**, since KW's traditional ordering is normally only defined at the trigram/hexagram level, not an intermediate digram level.

3. ~~Sync outstanding TO-DOS.md items to Asana.~~ **Obsolete — Asana has been removed.** The user disconnected the Asana MCP integration and deactivated the Asana account entirely (2026-08-14); `TO-DOS.md` is now the sole task tracker with no external mirror. The "Disconnect Asana Integration" TO-DOS.md section has been removed as complete. Do not propose Asana syncing again.

4. **Four pre-existing, still-untouched TO-DOS items** (unchanged since before this session began, full detail in `TO-DOS.md`'s first section):
   - Annotate the XianTian trigram circle's own construction logic (self-reversing vs. mixed cross-axis, binary line values) — directly relevant to / a likely prerequisite for new item 2 above.
   - Build a trigram→64-hexagram **concentric-ring** diagram.
   - Link the trigram circle and hexagram circle diagrams together.
   - Fix the stale closing paragraph in `design.md` (references a deleted Figma file).

5. **Publish `diagrams/group-table.html`, `diagrams/kw-square.html`, and `diagrams/FX_circle_KW_square.html` as Claude Artifacts** (or decide not to) — none of the three has been published; all exist only as local files verified via headless-Chromium screenshots. `diagrams/FuXi21.html` is likewise local-only (pre-existing file, only edited in place).

6. **Wire the new pages into `index.html`'s Structure strand.** The "Group Table Properties" entry card in `index.html` is still the old unlinked `<div class="entry">`; no entry cards exist yet for `kw-square.html` or `FX_circle_KW_square.html`. This offer has been open/unanswered since earlier in the session (never explicitly declined) — worth re-raising.

7. **Update `notes/structure/Group-Table-properties.md`'s top link** — still points to the old, superseded Claude Artifact URL (`https://claude.ai/code/artifact/0c3aa836-a82e-4bde-b072-b9a87ecbf557`), should point at `group-table.html` instead.

8. **Behaviorally verify the UNIFY tab-reuse fix.** The `rel="noopener"` removal (item 17 above) was diagnosed from HTML spec/MDN knowledge, not confirmed empirically (headless single-page testing can't observe real multi-tab browser behavior). Next session (or the user directly) should click UNIFY on two different FX groups in the combined page and confirm only one "Groups Table" tab is used/refreshed rather than a new one opening each time.

</work_remaining>

<attempted_approaches>

- **Regex-based HTML patch on `group-table.html`** silently corrupted the file structure once (deleted a closing tag it shouldn't have touched); fixed by regenerating the whole file. Adopted as a standing rule for the rest of the session: regenerate whole files for structural HTML changes (via anchor-verified Python line-slicing, not loose regex), only use `Edit` for pure CSS-value tweaks.
- **Two rejected "reposition hexagrams" over-corrections** in `group-table.html` (full element-order reversal, then a smaller badge/glyph swap) — user explicitly flagged both as overshooting a "small adjustment"; reverted both, then only tuned gap sizes.
- **`text-align:center` for pair-row labels** in `group-table.html` broke prefix alignment across differing string lengths; replaced with a measured fixed `padding-left`.
- **Hand-typing the 64-entry `HEXLINES` data** directly into `FuXi21.html` produced 56/64 wrong entries — caught via byte-for-byte verification *before* shipping, not by the user. Established the pattern (reused successfully for all later copies of this data into `kw-square.html` and `FX_circle_KW_square.html`) of generating data literals programmatically and diffing them against the source, never hand-typing 64-entry tables again.
- **Assuming a CSS container's width equals its visible content's width** — false for the FX circle's SVG (only ~87% viewBox fill) when trying to match its diameter to the KW square's width. First attempt (set container = 480px) produced a visibly-smaller circle despite "correct" CSS numbers; required `svg.getBBox()` measurement of actual rendered content to compute the right container size. This was the second time in the session a "the numbers say it's right but the user says it looks wrong" report turned out to reveal a genuine measurement-methodology gap, not user error or caching (contrast with the item below).
- **First "circle isn't bigger" report (item 16, the +20% step)** was investigated thoroughly (CSS values, `getBoundingClientRect()`, pixel-bounding-box screenshot analysis) and found to be *correct* — concluded this particular instance was most likely a browser/pCloud-sync caching issue on the user's end, since a hard refresh didn't resolve it either. Contrast with the diameter-matching bug immediately after, which *was* a real bug. **Lesson: don't assume every "I don't see the change" report is caching — verify thoroughly each time, since some are real bugs and some aren't, and they can occur back-to-back.**
- **3-column CSS Grid auto-centering for the UNIFY button** — placing it in a middle grid column with `justify-self/align-self:center` looked correct conceptually but was measurably off (`dx=59.4px, dy=72.5px`) because the flanking columns' widths are driven by the much-wider circle/square content, not by the smaller boxes centered within them — the grid column's geometric center doesn't coincide with the true midpoint between the box edges. Replaced with direct JS-computed absolute positioning from real `getBoundingClientRect()` values.
- **`pointer-events:none` on only the `<svg>` element** (first attempt at fixing the click-away dead-zone bug) was insufficient — the identically-sized parent `<div class="stage">` still captured the click. Had to apply `pointer-events:none` to the actual outer wrapper matched by the deselect logic's `.closest()` check (`.fx-diagram` / `.stage`), with `pointer-events:auto` re-enabled specifically on `.num-label`.
- **`target="_blank"`** was tried first for UNIFY (opens a new tab every click) — user clarified they wanted tab *reuse*, not a fresh tab each time.
- **`target="groups-table"` + `rel="noopener"` together** — looked like it should work (named target = standard tab-reuse mechanism) but didn't, because `rel="noopener"` specifically coerces non-`_top`/`_self`/`_parent` targets back to always-new-tab behavior per the HTML spec. Removing `noopener` (safe here, same-origin trusted local files) was the actual fix.
- **Chromium headless reading/writing under `/tmp/...scratchpad/...`** continues to fail in this sandbox (confirmed again this stretch) — all headless Chromium I/O (screenshots, `--dump-dom`) must use paths under `$HOME`. Disposable debug directories (`/home/greg/fxkw-debug/`, `/home/greg/click-debug/`, etc.) were created and `rm -rf`'d repeatedly throughout — safe pattern, keep using it.

</attempted_approaches>

<critical_context>

- **Still no git repo.** All persistence is direct file writes to the pCloud-synced project directory; no undo beyond manual care.
- **Verification standard held rigorously all session** — every sizing/positioning/color/behavioral claim was checked with real `getBoundingClientRect()`/`getBBox()`/computed-style measurements or simulated-click behavioral tests, never eyeballed from a screenshot alone or trusted from arithmetic alone. This caught at least 3 genuine bugs this final stretch alone (the SVG-viewBox-padding diameter bug, the UNIFY-button grid-centering bug, and the pointer-events dead-zone bug) that would have looked fine in a casual screenshot check.
- **CSS `zoom` scales `margin` by the same factor as visual size.** Reused correctly again this stretch for the display-box shrink+raise on `FX_circle_KW_square.html` (raw `margin-top:-200px` at `zoom:0.5` → effective `-100px`, verified exactly).
- **SVG content does not necessarily fill its own viewBox/container.** The FX circle's drawn rings+glyphs+numbers occupy only ~87% of their 1000×1000 viewBox (869.9-unit content bbox); this is baked into the circle's coordinate geometry (inherited from `FuXi21.html`'s original design, not something introduced this session). Any future work matching this circle's *visual* size to another element must measure via `svg.getBBox()` + the SVG's rendered scale factor, not just set the container's CSS width.
- **`pointer-events` cascades matter for "click blank area" logic.** A parent element with a real layout box (even if visually mostly empty/unpainted) still captures clicks anywhere within its box by default — setting `pointer-events:none` deep on a leaf element (e.g. just the `<svg>`) doesn't help if an identically-sized ancestor still captures the click first. Must set it on whichever ancestor the deselect-logic's `.closest()` check actually matches against, then explicitly re-enable `pointer-events:auto` on the real interactive leaf elements.
- **`rel="noopener"` + a named (non-`_blank`) `target` don't mix** — per the HTML living standard / MDN, `noopener` coerces any target other than `_top`/`_self`/`_parent` into `_blank`-like "always open a new context" behavior, defeating same-name tab reuse. Only use `noopener` with genuinely external/untrusted links, or omit it when same-origin tab-reuse is wanted.
- **All 64-entry datasets (`HEXLINES`, `GROUPS`, and the `FX_TO_KW`/`KW_TO_FX` bijection) are now duplicated by copy-paste across four separate files** (`FuXi21.html`, `kw-square.html`, `group-table.html`, `FX_circle_KW_square.html`) rather than shared via any import mechanism (there's no build step / module system in this static-file project). Every copy has been independently byte-for-byte verified against the canonical source this session, but **any future correction to this data must be manually propagated to all four files** — there is no single source of truth file to edit once.
- **The FX-circle's `GROUPS[i].pairs` grouping is neither pure zong (reversal) nor pure cuo (complement)** but the Klein-four-group's "diagonal" combined relationship — established in an earlier part of this session, still relevant if the new trigram-circle-pairing work (TO-DO item 2) ends up needing an analogous grouping definition at the trigram level.
- **`FX_circle_KW_square.html`'s FX-circle highlight color was unified to `--structure`** (matching the KW square) **but `FuXi21.html`'s own standalone palette (`--seal`, `--verify`) was deliberately left untouched** — these are now two different color schemes for what is visually "the same" FX circle, depending on which page you're looking at. This was an explicit, scoped instruction ("the highlight colour on the FX diagram the same as that of the KW" — read as applying to the combined page only), not an oversight, but worth remembering if the two pages are ever meant to look identical.
- **User's collaboration style, held consistently across the entire session**: extremely hands-on, precise, incremental, geometry/behavior-driven instructions delivered one at a time ("increase by 30%", "raise by 100px", "centred horizontally and vertically"), explicit and specific correction when a change overshoots or misses intent, direct build→verify→show→iterate rhythm strongly preferred over planning/spec ceremony (established explicitly mid-session, held for the rest of it). The user does *not* accept "it should work" — wants concrete verification, and multiple times this session correctly pushed back when a change was claimed-correct-but-not-actually-so, which surfaced real bugs each time.
- **Two apparent "the file didn't update" reports this session had different root causes**: one was genuinely just browser/sync caching (the +20% circle-size step — thorough measurement confirmed the file was correct), the other two ("diameter doesn't match", "UNIFY still opens a new tab") were real bugs each time. Don't default to blaming caching — verify fully every time.

</critical_context>

<current_state>

- **`diagrams/group-table.html`**: complete — formatting, hover/click/Ctrl+click multi-select, `?fx=` URL param pre-selection (for the UNIFY link), `clearAllSelected()`, click-away-to-deselect. Not published as a Claude Artifact, not linked from `index.html`.
- **`diagrams/kw-square.html`**: complete — formatting, full display-box/highlight parity with `FuXi21.html`, click-away-to-deselect. Not published, not linked from `index.html`.
- **`diagrams/FuXi21.html`**: complete — display box, halved highlight scope, spoke removal, +15% circle size, click-away-to-deselect with the pointer-events dead-zone fix applied. Retains its own original `--seal`/`--verify` palette (not unified with the other pages). Pre-existing file, edited in place throughout, no separate "new artifact" version.
- **`diagrams/FX_circle_KW_square.html`**: complete, brand-new artifact built entirely this final stretch — FX circle (visible diameter matched to KW square, then explicitly enlarged 30% beyond that) and KW square side by side, synchronized click-selection with matching `--structure`-colored highlighting on both sides, dual display boxes (shrunk 50%, precisely repositioned), a UNIFY button exactly centered between the boxes (JS-computed position) linking to `group-table.html?fx=...` in a reused named tab, and click-away-to-deselect with the same dead-zone fix as `FuXi21.html`. Not published as a Claude Artifact, not linked from `index.html`.
- **`TO-DOS.md`**: 7 sections total — the original 4 (untouched, still open), 2 now-**stale/completed** sections not yet cleaned up (`Combined FX Circle + KW Square Artifact`, `Bring KW Square Up to Parity with FX Circle`), and 1 brand-new section (`Trigram Circles, 3D Cubes & Central Taiji Page`, 4 items, 2 of which explicitly need user clarification before design work can start).
- **Asana**: removed entirely (2026-08-14) — MCP integration disconnected and the Asana account deactivated. The 4 gids created earlier in this session are historical only (dead references, account no longer exists). `TO-DOS.md` is now the sole task tracker.
- **`whats-next.md`**: this document — full rewrite covering the entire session from the original resume-point through this final stretch, superseding the prior end-of-day checkpoint.
- **No open questions blocking immediate next steps**, but two of the four newly-added TO-DO items (the 3D-cube "Z.D. Yung" reference, and the "FX/KW digrams" concept for the Taiji page) are explicitly flagged as needing user clarification *before* any design work begins on them — don't guess at these, ask first.

</current_state>

<reading_instructions>
Whoever reads this document — a fresh Claude instance resuming this work, at the start of a new session — must lead their first response to the user with the contents of <current_todos> below, before summarizing <original_task>, <work_completed>, or anything else in this document.
</reading_instructions>

<current_todos>
# TO-DOS

## Finish integrating FX-circle-unfolding.html from the 2026-08-22 (evening) session - 2026-08-22

- **A new animated diagram, `diagrams/FX-circle-unfolding.html`, was designed, built, and verified this session, but is not yet linked from anywhere else in the project.** It shows the FX circle growing outward from the FX1/FX2 axis, one line at a time, across 193 manually-stepped states (1 static + 6 passes × 32 positions), reusing the two-pole complementary-unfolding structure verified in `notes/structure/FX-circle-properties.md`. Built via `superpowers:brainstorming` (classified bounded), with the user confirming scope (full 64-position build), controls (manual Prev/Next, not auto-play), and placement (new standalone page) before implementation.
- **Verified working** (headless Chromium screenshots, since Playwright's own Chromium wasn't installed — used the system `chromium-browser` with `--no-sandbox` instead, screenshots written under `$HOME` since the snap-confined browser can't write to `/tmp`): static start, first reveal, pass-2 correctly resetting to the same 32 positions and adding a second ring, and the completed 6-ring final state. **Also fixed mid-session:** the user caught that individual line-marks weren't laterally separated (arc-length per position was smaller than mark width, so rings looked like solid stripes) — fixed by pushing all 6 radii out (130–290 &rarr; 340–460) and enlarging the viewBox (1000&times;1000 &rarr; 1100&times;1100); re-verified at close zoom that yang (solid) vs yin (split) marks are now clearly legible on all 6 rings.
- **One known, explicitly-flagged trade-off, not yet resolved with the user:** the animation's first revealed position is FX15/FX16, not FX1/FX2, even though FX1/FX2 anchor the drawn axis and the bar's start angle. This is because FX1 sits 7 steps *into* the yang block rather than at its edge (verified from the real circle's geometry) — starting the sweep exactly at FX1 would force the clean block-doubling pattern (16/16, 8/8/8/8, etc.) to break into a messier 3-piece pattern with a seam. The trade-off was explained to the user (prioritizing the clean verified structure over literal FX1-first ordering) but not yet explicitly confirmed as acceptable — worth checking next session, and revisiting the geometry if the user would rather have literal FX1-first ordering even at the cost of a seam.
- **Not yet done:**
  - Not linked from `index.html`'s "Structure" strand, nor from the footer nav of the other diagrams (`FX_circle_KW_square.html`, `FX_KW_spine.html`, `group-table.html`) — currently one-directional (this page links out to those three, they don't link back).
  - Not tested live by the user in an actual browser — only verified via headless-Chromium screenshots this session.
  - Mark width is currently fixed (22px yang / 8+3+8px yin) regardless of ring radius, so the innermost ring (L1) has tighter visual spacing than the outermost (L6) — not a bug, just uneven; worth a look if it reads as inconsistent once seen live.
- **Files:** `diagrams/FX-circle-unfolding.html` (the new page); `notes/structure/FX-circle-properties.md` (the verified structure it's built from — its "Two-pole complementary unfolding" table and regeneration method are the direct source of `TOP_ARM`/`BOTTOM_ARM`/`HEXLINES` embedded in the new page).

## Write up Group-Table-properties.md findings 10–16 from the 2026-08-21 session - 2026-08-21

- **A substantial block of exhaustively-verified structural findings about the FX Group Table and FX numbering scheme was produced across two sessions (2026-08-21) but still has not been written into any project file.** **Problem:** the first 2026-08-21 session (structural brainstorming on `notes/structure/Group-Table-properties.md`) established, computationally, and left undocumented: the exact-opposite-pair law for all 32 FX pairs (every FX(2k-1)/FX(2k) pair is a full line-by-line inversion); the precise "yang hexagram = line 1 is yang" criterion (zero exceptions, vs. the "majority yang lines" framing which was tested and explicitly rejected — 10 ties + 5 reversals out of 32); the fact that every one of the table's 20 groups is composed entirely of whole opposite-pairs, which explains three of the table's other properties at once; a King-Wen-numbering control test proving the FX/table relationship is not generic to any numbering (FX passes all three tests exactly, KW fails at/near chance level — the single most load-bearing piece of evidence produced); and the terminology decision to call the FX numbering "faithful" (structure-preserving), explicitly adopted as this project's own stated design rationale going forward, not as a claim about recovered historical intent (that historical-provenance question remains open and skeptical in `notes/culture/Fuxi-sequence-origins.md`, and must not be conflated with the going-forward design-rationale claim). A later 2026-08-21 session added a related but separate analysis (Observation 10, now written) characterizing the Group Table's 20 groups as the Klein four-group orbit partition under cuogua (complement) and zonggua (reversal) — that part **is** now written up. **Files:** `notes/structure/Group-Table-properties.md` (likely home for the mathematical/structural findings, as further numbered Observations); `notes/culture/Fuxi-sequence-origins.md` (likely home for the historical-rationale framing, extending its existing "Implications for this project" section around line ~192-196). **Solution:** ask the user where this should go (a single location, or split as sketched above with cross-links), then write it up — all underlying data is independently re-derivable from `diagrams/group-table.html` / `diagrams/FX_circle_KW_square.html`'s own rendered/embedded data per this project's verification standard, so nothing here depends on unreliable memory.

## Footer nav links don't reuse an already-open tab in Firefox - 2026-08-20 14:11

- **Named-target footer links (`diagrams/FX_KW_spine.html`) aren't reusing an already-open destination tab in the user's actual Firefox browser, even though the tab was itself opened via the same link.** The three footer buttons — "Yijing Pathways", "FX circle / KW square", "Groups table" — each got a `target="..."` attribute this session (`target="yijing-pathways"`, `target="fx-circle-kw-square"`, `target="groups-table"`) specifically so that clicking one when its destination page is already open in another tab would return to/focus that tab instead of opening a new one, matching the pre-existing convention already used elsewhere in this project (`FX_circle_KW_square.html`'s own `target="fx-kw-spine"` and `target="groups-table"` links). **Problem:** verified working correctly in headless Chromium (`context.pages().length` stayed at 2 after clicking the same link twice — see the session's `test_targets.js` in a now-gone scratchpad, easy to recreate) — but the user, testing live in Firefox, reported the link still opens a brand-new tab on a second click, even after explicitly confirming (via the session's `AskUserQuestion`) that the already-open tab *was* itself opened by clicking that same link earlier — which was expected to be the one case where reuse should reliably work. The user had to leave mid-diagnosis, so this is unconfirmed/unresolved, not yet root-caused. **Files:** `diagrams/FX_KW_spine.html` (the three footer links, this session's new `target=` attributes); `diagrams/FX_circle_KW_square.html` (pre-existing `target="fx-kw-spine"`/`target="groups-table"` links on `spine-link-fx`/`spine-link-kw`/`unify-link` — worth checking whether *this* older pre-existing target-reuse behavior also fails for the user in real Firefox, or whether it's specific to the newly-added links, since that would narrow down whether this is a Firefox quirk with named targets in general vs. something specific to this session's change). **Solution:** not yet found — next session should first try to get a precise, minimal repro (e.g. a two-link toy HTML file) to isolate whether this is a Firefox-specific bug/setting around named-window-target matching (there's a real, documented Firefox preference `browser.link.open_newwindow` and related tab-vs-window handling that can affect this), whether it's specific to `file://` origins (already established this session that Firefox isolates each `file://` URL as its own origin, which broke other cross-tab approaches — worth checking if that isolation also affects named-target matching, not just storage/messaging APIs), or whether the *existing* `fx-kw-spine`/`groups-table` targets on `FX_circle_KW_square.html` have the exact same problem and it's simply never been noticed before. Ask the user to check Firefox's `about:preferences#general` tab-opening settings, and/or test target-reuse with a trivial two-page reproduction outside this project's actual files to isolate the variable.

## XianTian Circle Diagrams & Hexagram Group Table - 2026-08-12 13:01

- **Show the logical/systematic design of the XianTian trigram circle** - Make the construction logic of the trigram circle visible to a reader, not just its arrangement. **Problem:** The trigram circle (hero SVG in `index.html`, and the original `diagrams/xiantian-trigram-circle.svg`) currently shows the 8 trigrams correctly positioned and color-coded (self-reversing pair vs. mixed pair via two cross-axes), but a reader can't tell *why* they're arranged this way just by looking — the binary/yin-yang line logic and the self-reversing/mixed grouping aren't explained. **Files:** `index.html` (hero SVG, trigram glyphs now drawn as precise rects), `diagrams/xiantian-trigram-circle.svg`, `diagrams/FuXi21.html`. **Solution:** consider annotating the two cross-axes directly (label what "self-reversing" and "mixed" mean structurally), and/or surfacing the binary line values (111/110/101/100/011/010/001/000) that drive each trigram's position. **UPDATE (2026-08-21 sessions):** substantial work directly relevant to this TODO is now done and written up — it exhaustively verified exactly which trigrams sit where and why (yang tetrad {Zhen,Li,Dui,Qian} = line-1-yang, occupies a clean contiguous 180° semicircle; yin tetrad {Xun,Kan,Gen,Kun} occupies the complementary semicircle — see `notes/structure/Group-Table-properties.md` and the still-unwritten findings 10-16 tracked in the new TODO item above), and separately, Observation 10 in that same file now gives the deepest available answer to "why this arrangement": the whole Group Table's 20 groups are exactly the orbit partition of the Klein four-group generated by cuogua (complement) and zonggua (reversal) acting on the hexagrams — a verified, non-arbitrary construction. None of this has been drawn into `xiantian-trigram-circle.svg` or `index.html`'s hero SVG yet, but the underlying "why" is now fully answerable and should make this TODO much faster to execute. **UPDATE (2026-08-22 evening session):** a real, working animated diagram now exists doing exactly this kind of "show the construction logic" work, though for the hexagram circle rather than the trigram circle specifically — see `diagrams/FX-circle-unfolding.html` (tracked in its own TODO item above) and the underlying verified geometry in `notes/structure/FX-circle-properties.md`. The same technique (SVG rect-based line glyphs, positioned by verified angle data, revealed step by step) is directly reusable if this TODO's trigram-circle version gets built.

- **Show how the XianTian trigram circle maps concentrically outward to the XianTian hexagram circle** - Build a new diagram showing the traditional concentric-ring construction: inner ring = 8 trigrams, outer ring = 64 hexagrams. **Problem:** no existing diagram in this project shows this mapping — it's a structural relationship central to the FuXi/XianTian sequence work but currently undocumented visually. **Files:** new diagram, most likely alongside `diagrams/xiantian-trigram-circle.svg` / `diagrams/FuXi21.html`; may draw on `data/spreadsheets/FX-01.ods` for FX hexagram ordering data. **Solution:** reuse the rect-based trigram glyph technique from `index.html`'s hero (font-independent, pixel-precise) for the inner ring; verify hexagram line patterns the same way they were verified for the trigrams (render and read directly, don't recall from memory) before drawing the outer ring.

- **Link the trigram circle and hexagram circle diagrams together** - Once both diagrams above exist, connect them as one coherent piece rather than two independent artifacts. **Problem:** not yet decided whether this means a single combined SVG, or two separate pages/entries cross-linked. **Files:** wherever the two diagrams from the items above end up (likely `diagrams/`), plus the "Structure" strand entries in `index.html`. **Solution:** decide the linking approach once both diagrams are built.

- **Rebuild the hexagram Group Table starting from a spreadsheet** - Redo the Group Table work, this time using a spreadsheet as the canonical starting data source rather than the prior approach. **Problem:** the existing `notes/structure/Group-Table-properties.md` needs to be reworked from scratch with a spreadsheet-first workflow. **Files:** `notes/structure/Group-Table-properties.md`, `data/spreadsheets/FX-01.ods`, `data/spreadsheets/FX-01.xml`. **Solution:** use `data/spreadsheets/FX-01.ods` (or a fresh spreadsheet, if that one isn't the right starting point) as the source of truth for regenerating the group table data and derived properties. **Note:** the 2026-08-21 sessions added substantial new *analytical* content to `Group-Table-properties.md` on top of the existing spreadsheet-independent version — Observations 7-9 (pure/centered/mixed trigram categories, center-to-periphery generation, FX-sum decomposition) and Observation 10 (the Group Table's 20 groups as Klein four-group orbits under cuogua/zonggua), plus the still-unwritten findings 10-16 tracked separately at the top of this file. A future spreadsheet-first rebuild should make sure all of this survives the rebuild or gets re-verified against the new source.

## Trigram Circles, 3D Cubes & Central Taiji Page - 2026-08-13 22:08

- **Build a graphical representation of the FX and KW trigram circles together** - Create a new diagram showing the FuXi (XianTian) trigram circle and the King Wen (HouTian/latter-heaven) trigram circle side by side or otherwise paired, analogous to `diagrams/FX_circle_KW_square.html`'s FX-hexagram-circle/KW-hexagram-square pairing built this session. **Problem:** no diagram in this project currently shows the two trigram orderings (FuXi vs King Wen arrangement of the 8 trigrams) together or in comparison — only the single FuXi trigram circle exists (`diagrams/xiantian-trigram-circle.svg`, and the hero SVG in `index.html`). **Files:** new diagram (likely `diagrams/fx-kw-trigram-circles.html` following this session's naming convention), `diagrams/xiantian-trigram-circle.svg` (existing FX trigram circle to reuse/extend), `index.html` (hero SVG has the established rect-based trigram-glyph rendering technique — reuse it rather than reinventing). **Solution:** the King Wen trigram arrangement (post-heaven/HouTian bagua) is a different, well-known traditional ordering from the FuXi (pre-heaven/XianTian) one used in this project so far — will need its own verified trigram-position data, sourced and cross-checked the same rigorous way the FX↔KW hexagram bijection was (see `notes/structure/FX_to_KW.md`), not assumed from memory.

- **Show how the yin/yang groups between the FX and KW trigram circles relate** - Once both trigram circles exist, add the same kind of group-highlighting/equivalence feature built this session for the hexagram FX circle ↔ KW square (click-to-highlight, matching color, `UNIFY`-style cross-link) — but for the 8 trigrams' yin/yang groupings rather than the 64 hexagrams' quartets/pairs. **Problem:** not yet designed — unclear exactly what "yin/yang groups" means at the trigram level in this project's terms (e.g. pure-yang/pure-yin trigrams vs. mixed ones, or the FuXi self-reversing/mixed cross-axis grouping already noted as unexplained in the pre-existing "Show the logical/systematic design of the XianTian trigram circle" TO-DO above). **Files:** whatever new diagram is built for the item above; `diagrams/FX_circle_KW_square.html` as the interaction-pattern reference (its `showGroup`/`clearGroup`/`HEX_TO_GROUP`/display-box/`UNIFY`-link machinery). **Solution:** resolve the pre-existing open question about the trigram circle's self-reversing-vs-mixed cross-axis grouping first (see the first item in the "XianTian Circle Diagrams & Hexagram Group Table" TO-DO section above), since that's likely the same "yin/yang group" structure this item wants surfaced and compared against the KW trigram ordering. **UPDATE (2026-08-21 sessions):** this is now largely resolved in substance (not yet in any diagram) — see the pure/centered/mixed trigram categories (`Group-Table-properties.md` Observation 7), the separate yang/yin-by-line-1 semicircle split (Zhen/Li/Dui/Qian vs Xun/Kan/Gen/Kun, part of the still-unwritten findings 10-16 tracked at the top of this file), and Observation 10's Klein four-group characterization (cuogua/zonggua orbits) — all directly applicable here.

- **Create 3D cube representations of the trigrams, "à la Z.D. Yung"** - Build a new visual/diagram representing each of the 8 trigrams as a 3D cube. **Problem:** the specific reference ("Z.D. Yung") and exact cube convention intended is not yet clear from this conversation alone — needs clarification from the user before design work starts (e.g. whether it's a known published diagram/book the user has in mind, what axes map to which of the 3 lines, whether yang/yin lines become filled/open cube faces or vertices, etc.). **Files:** new diagram, likely `diagrams/trigram-cubes-3d.html`; may want CSS 3D transforms (`rotate3d`/`perspective`) or an SVG-based pseudo-3D projection consistent with this project's existing SVG-rect line-glyph technique rather than pulling in a 3D library/dependency. **Solution:** ask the user for the source/reference for "Z.D. Yung" and the intended cube convention before implementing, since guessing wrong here would waste a full design-and-build cycle.

- **Build a central-Taiji page linking FX trigrams ↔ FX digrams ↔ Taiji ↔ KW digrams ↔ KW trigrams** - New page with the Taiji (yin-yang) symbol at its center, flanked outward on one side by FX digrams then FX trigrams, and on the other side by KW digrams then KW trigrams. **Problem:** "digrams" (2-line figures, the 4 possible combinations of two stacked yin/yang lines) are a new concept not yet represented anywhere in this project — only trigrams (3-line, 8 combinations) and hexagrams (6-line, 64 combinations) exist so far, so the digram data/ordering (both FX and KW variants) needs to be defined and verified from scratch, the same rigorous way trigram and hexagram data has been throughout this project (derive and cross-check programmatically, don't hand-type). **Files:** new page, likely `diagrams/taiji-trigrams-digrams.html`; `index.html` hero SVG and `diagrams/xiantian-trigram-circle.svg` for the existing rect-based line-glyph rendering technique to extend down to 2-line digrams and up to reused 3-line trigrams. **Solution:** clarify with the user what "FX digrams" vs "KW digrams" actually means (KW's traditional sequence is normally defined at the trigram/hexagram level, not digram level — the FX/KW digram distinction may need to be derived by the user's own logic, not an existing classical source) before building; likely depends on the "yin/yang groups" work above being resolved first, since the whole point of this page is showing the progressive binary construction (Taiji → digrams → trigrams) symmetrically for both orderings.

## KW Square Distance Markers - 2026-08-14 14:15

- **Add counting markers and distance brackets to the KW square** - Give `diagrams/kw-square.html` the same per-group distance annotation that `diagrams/FX_circle_KW_square.html` already has on the FX circle side. **Problem:** `FX_circle_KW_square.html` draws, per selected group, counting markers plus a bracket between paired members showing their distance apart (`buildDistanceLink()` around line 1752, drawing into the `#distance-overlay` `<g>`, populated on `showGroup()` and cleared via `overlay.innerHTML = ''` in `clearGroup()` — see the `.distance-link`/`.distance-tick`/`.distance-label-bg`/`.distance-label-text` CSS around line 126). `kw-square.html` has no equivalent yet. **Files:** `diagrams/kw-square.html` (to build); `diagrams/FX_circle_KW_square.html` (reference implementation, `buildDistanceLink()` and the `showGroup`/`clearGroup` pop-up wiring). **Solution:** port the same show-on-select/clear-on-deselect behavior, but note the KW square is a square grid, not a circle — `buildDistanceLink()`'s arcs (`arcPath()`, `.distance-link` paths) don't apply directly. The KW brackets need to be **straight lines with tick marks** (not arcs) — keep the tick/label-bubble parts of the pattern, replace the arc geometry with line geometry appropriate to the square layout.
</current_todos>

<original_task>
This session opened with the user reading the prior 2026-08-22 handoff (which had closed out a clean, pushed state after writing the trigram-pair-perspective subsection to Observation 11). From there this session had three connected phases, all continuing the FX-circle structural investigation:

1. **Property-1/2/3 geometric investigation, corrected twice by the user.** The user asked to look again at "Property 3" (the line-doubling pattern), first as a "barred spiral galaxy" analogy, then refined it into a precise nested "digram" left/right formula. Claude twice got the framing wrong — first claiming the pattern "breaks down" past line 3 (based on the wrong single-bit-adjacency test), then claiming a "polarity flip" existed between the inner and outer trigram (based on comparing the two arms in the wrong relative reading direction) — and the user corrected both errors directly ("your analysis is not correct; the pattern holds right through" / "I don't understand your 'opposite rotational senses'... both moving in a counter-clockwise direction"). Both corrections were verified computationally once reconsidered, and both turned out to be real errors in Claude's framing, not the user's. The final, correct, fully-verified finding: reading both arms outward from FX1 and FX2 in the **same** counter-clockwise direction, the bottom arm's hexagram is the exact line-by-line complement of the top arm's hexagram at every matching step, for all 6 lines — a direct, position-by-position restatement of the already-established diametric-complement law (Property 1), not a separate reflection rule.

2. **Write-up into a new file, `notes/structure/FX-circle-properties.md`.** The user asked for the corrected table, then directed "top priority" — put this into "the FX-circle properties document." No such file existed, so Claude created it fresh (distinct from `Group-Table-properties.md`, which covers the FX Group Table's row/grouping structure, not the circle's own geometry), containing: the regeneration method, Property 1 (diametric complements), Property 2 (the 8×8 trigram grid, refined), Property 3 (the corrected two-pole table), the inner-trigram 3-bit reflected Gray cycle, and a low-priority aside on "antipodal Gray codes" (a real, named, actively-studied combinatorial object found via `WebSearch` — the FX circle's inner trigram is specifically a (3,4)-antipodal Gray code). `Group-Table-properties.md`'s own prior aside on the same topic was replaced with a cross-link to avoid duplication. `TO-DOS.md`'s "top" item (tracking this unwritten work) was removed since it's now resolved.

3. **A new animated diagram, `diagrams/FX-circle-unfolding.html`, built via `superpowers:brainstorming`.** The user asked to conceive the circle as a "dynamic unfolding" — a stepwise animation of a rotating central bar with yin/yang lines appearing at both poles simultaneously. After a paste mishap (resolved by the user saving the full text to `/home/greg/Desktop/Yijing/FX-circle-animation/FX-circle-animation.txt` for Claude to `Read` directly) and several rounds of scope-clarifying questions (full 64-step build vs. partial; manual step-through vs. autoplay; new page vs. new mode; full-hexagram-per-position vs. one-line-at-a-time growth — all resolved via `AskUserQuestion`), Claude designed and built the page, verified it via headless-Chromium screenshots (Playwright's own browser wasn't installed; system `chromium-browser --no-sandbox` was used instead, with output written under `$HOME` since the snap-confined browser couldn't write to `/tmp`). The user then caught a real visual bug (line-marks not laterally separated, reading as solid stripes) — fixed by pushing the ring radii out substantially and enlarging the viewBox, re-verified at close zoom. The user then said they have to close for the day (resuming tomorrow on a different computer, "mac17") and gave the standard three-step closing instruction — update TO-DOS, refresh whats-next, commit and push — which this document and the following commit are executing.
</original_task>

<work_completed>

## 1. Two framing errors caught and corrected (Property 3 investigation)

- **First error:** claimed the two-pole doubling pattern "does not extend to lines 4–6," based on testing single-line-flip (Hamming-distance-1) adjacency between *consecutive* geometric positions — a different, stricter property than the one actually being asked about. The user's exact original text (recovered from `/home/greg/Desktop/Yijing/FX-circle-animation/FX-circle-animation.txt` reading — no, this was the *prior* correction, sourced from the conversation directly) said plainly: "the repetition is intentional... only after the first 32 steps does the next cycle (line-2) begin."
- **Second error:** after re-testing and finding what looked like a "polarity flip" between the inner (lines 1-3) and outer (lines 4-6) trigram halves when comparing top-forward to bottom-forward reading, Claude presented this as "your table needs one correction: the polarity flips at line 4." The user rejected this too: "I don't understand your 'opposite rotational senses'... both moving in a counter-clockwise direction." Re-verified with the correct comparison (same-direction reading, not reversed): `bottom[k]` is the *exact full complement* of `top[k]` for all 6 lines, at every k, zero exceptions — the "flip" Claude saw was an artifact of comparing top-forward against bottom-forward without accounting for the fact both arms move the same way, not that one is the mirror-reverse of the other.
- **Lesson, worth remembering for future sessions on this material:** the correct relationship is the *simpler* one (direct complementation, same reading direction) — both of Claude's wrong framings this session were more complicated than the truth. When re-deriving a claimed structural relationship and finding something "almost but not quite" matching what's already established, re-check the comparison basis itself before concluding the pattern breaks or needs a correction — both times this session, the error was in the comparison, not in the underlying structure.

## 2. `notes/structure/FX-circle-properties.md` — new file, written and cross-linked

Created fresh (no such file existed before). Contents: regeneration method (parsing `diagrams/FX_circle_KW_square.html`'s wedge rotations and per-glyph angles directly, not assuming); Observation 1 (diametric complements, confirmed exactly and geometrically for all 32 pairs); Observation 2 (the 8×8 trigram grid — inner trigram forms the 8 canonical 45° macro-sectors, outer trigram's sub-order is inherited from the pre-existing FX numbering, not independently circular); Observation 3 (the corrected two-pole complementary-unfolding table, exactly as the user wrote it, with the "same direction, direct complement" relationship stated precisely); Observation 4 (the inner trigram's exact 3-bit reflected Gray cycle, and where the full 6-bit version breaks down — Hamming distance 1 for only 34/64 steps); an aside on "antipodal Gray codes" (sourced via `WebSearch`: a real, named, actively-studied family, originating from a problem posed by Hunter Snevily, with existence results by Killian and Savage and a dedicated *Theoretical Computer Science* paper on the general (n,t) family — this circle's inner trigram is the **t = 2ⁿ⁻¹** case, i.e. a (3,4)-antipodal Gray code). `Group-Table-properties.md`'s own duplicate aside on the same topic was replaced with a cross-link. Heading structure verified clean in both files via `grep -n "^#\|^##\|^###\|^####"` after every edit.

## 3. `diagrams/FX-circle-unfolding.html` — new animated diagram, built and verified

- **Design, via `superpowers:brainstorming` (classified bounded)**: clarifying questions resolved via `AskUserQuestion` — full 64-step build (not a partial demo), manual Prev/Next step-through (not autoplay), new standalone page (not a mode on the existing circle page), one-line-at-a-time two-dimensional growth (not full-hexagram-per-position). A mid-design ambiguity (whether step 3 of the user's description was a duplicate-by-mistake or intentional) was resolved by asking directly — the user confirmed the repetition across the first 32 steps is intentional and correct.
- **Implementation**: `TOP_ARM`/`BOTTOM_ARM` (32-element FX-number arrays, verified exact complements at every index) and `HEXLINES` (all 64 hexagrams' line data) embedded directly, both re-derived fresh from `diagrams/FX_circle_KW_square.html` rather than typed from memory. 193 total steps (1 static + 6 passes × 32 reveals — corrected from an initial miscount of 194, caught by re-reading the user's original file text precisely). Each pass re-sweeps the same 32 angular positions and adds one new concentric ring (line) per pass, per the user's explicit design choice.
- **Verification method**: Playwright's own Chromium wasn't installed and couldn't be installed without an interactive sudo prompt; used the system `chromium-browser --headless --no-sandbox --screenshot=...` instead. Discovered mid-session that the snap-confined browser can't write to `/tmp` (writes silently vanish) — screenshots and temp test-copy HTML files had to go under `$HOME` instead. Verified: static start (step 0), first reveal (step 1, FX15/FX16 at line 1), pass-2 start (step 34, resets to the same position and adds a second ring, prior ring still fully drawn), and the completed final state (step 193, all 6 rings, Next/Last buttons correctly disabled).
- **Bug found and fixed by the user**: individual line-marks weren't laterally (arc-length) separated at the original ring radii (130–290 in a 1000×1000 viewBox) — arc-length per 5.625° step was smaller than the mark's own width, so adjacent marks visually merged into solid-looking stripes instead of discrete hexagram-line glyphs. Fixed by pushing all 6 ring radii out to 340–460 and enlarging the viewBox to 1100×1100 (bar length, spoke length, and pole-label positions updated to match). Re-verified via a cropped, upscaled screenshot that individual yang (solid bar) and yin (split bar) marks are now clearly legible on all 6 rings, and that the values shown match `HEXLINES` exactly (spot-checked against FX15's known line pattern).
- **One explicitly-flagged, not-yet-resolved trade-off**: the animation's first revealed position is FX15/FX16, not FX1/FX2, because FX1 sits 7 steps into the yang block rather than at its edge (verified from the real circle's own geometry) — starting the sweep exactly at FX1 would force the clean block-doubling pattern to break into an uneven 3-piece pattern with a seam. Explained to the user but not yet explicitly confirmed as an acceptable trade-off.

## 4. TO-DOS.md and this document updated (this message's request, steps 1–2 of 3)

Added a new top TODO section ("Finish integrating FX-circle-unfolding.html...") capturing everything not-yet-done about the new animation (not linked from other pages, not tested live by the user, the FX1/FX2-vs-FX15/FX16 trade-off, the uneven mark-width-vs-radius issue). Added a short UPDATE annotation to the pre-existing "Show the logical/systematic design of the XianTian trigram circle" TODO item, pointing at the new animation as directly relevant prior art. This document itself is step 2. Step 3 (commit and push) is next.

</work_completed>

<work_remaining>

## Immediate: step 3 of the user's three-step instruction — commit and push

Steps 1 (`TO-DOS.md`) and 2 (this `whats-next.md`) are done. **Step 3 — commit and push — has not been done yet.** Commit should cover:
- `TO-DOS.md` (new top item, one UPDATE annotation, this session)
- `notes/structure/FX-circle-properties.md` (new file, this session)
- `notes/structure/Group-Table-properties.md` (aside replaced with cross-link, this session)
- `diagrams/FX-circle-unfolding.html` (new file, currently untracked — needs `git add`, not just committing already-tracked changes)
- `whats-next.md` (this refresh, this session)

Then push to `origin/master` per this project's established `feedback_push-reminder`/`project_github-setup` pattern (public repo, GitHub Pages auto-deploys from `master`). Run `git status`/`git diff --stat` fresh before committing rather than trusting any numbers in this document.

## Cross-machine resume note

**The user is resuming tomorrow on a different computer ("mac17"), not this one.** This project lives on pCloud (`/home/greg/pCloudDrive/YIJING/Yijing-Pathways` on this machine — see `reference_pcloud-backups` memory), and per `project_github-setup` it's also a public GitHub repo pushed to `origin/master`. As long as step 3 above completes successfully *before* this session ends, the mac17 session should be able to pick up cleanly either via pCloud sync or via `git pull` on that machine — but this hasn't been explicitly verified working cross-machine before in this project's history (per `reference_pcloud-backups`, there's a known caveat that Obsidian-vault edits can lag behind the pCloud copy; worth bearing in mind if mac17 also runs Claude Code against a *different* local clone/sync path than this one). If starting fresh on mac17 and anything looks stale or out of sync, check `git log`/`git status` there before assuming this handoff's account of the repo state is current.

## Also still open

- **The FX1/FX2-vs-FX15/FX16 trade-off in `FX-circle-unfolding.html`** — not yet confirmed acceptable by the user. Ask directly next session if it hasn't come up already.
- **`FX-circle-unfolding.html` is not linked from anywhere** — `index.html`'s Structure strand and the other diagrams' footer navs don't point to it yet.
- **`FX-circle-unfolding.html` has not been seen live by the user** — only verified via headless-Chromium screenshots this session. Worth having the user actually click through it once, since the whole point was a visual/interactive artifact.
- **Findings 10–16 from the first 2026-08-21 session remain unwritten** — unchanged from prior handoffs, still tracked in its own TODO item, still the oldest substantial unwritten-analysis backlog item.
- **All of `<current_todos>` above remains open** except for this session's own additions/updates — the Firefox tab-reuse bug is still the longest-standing unresolved item, and most of the diagram-building TODOs (trigram circle annotations, concentric hexagram circle, FX/KW trigram-circle pairing, 3D cubes, Taiji/digram page, KW square distance markers) haven't been started, though several now have much stronger "why"/technique groundwork available than before (see the UPDATE annotations).

</work_remaining>

<attempted_approaches>

- **Two real analytical errors this session, both caught by the user, both confirmed as genuine mistakes on re-verification** — see `<work_completed>` item 1 for full detail. Worth remembering as a pattern: this session's errors were both about *comparison basis* (which direction/orientation to read two things in relative to each other), not about the underlying computed data being wrong. When a "correction" or "refinement" of an already-established structural claim is about to be presented, it's worth pausing to ask whether the discrepancy might instead be in how the comparison itself was set up.
- **Playwright's bundled Chromium wasn't available** (`Chromium distribution 'chrome' is not found`), and `npx playwright install chrome` failed non-interactively (needs a sudo password prompt). Worked around by using the system `chromium-browser` (a snap package) directly via Bash with `--headless --disable-gpu --no-sandbox --screenshot=...`. **Snap confinement blocks writes to `/tmp`** (screenshot files "written" there silently don't appear) — this cost some time to diagnose (first attempt reported success but `ls` couldn't find the file). Writing under `$HOME` instead works reliably. Worth remembering directly for any future headless-browser verification in this environment: use `$HOME`-based paths, not `/tmp`, when driving the system snap Chromium.
- **A user copy-paste problem mid-session** ("every time I paste the inserted text is no longer editable") was resolved by having the user save the full text to a local file (`/home/greg/Desktop/Yijing/FX-circle-animation/FX-circle-animation.txt`) and giving Claude the path to `Read` directly — much more reliable than continuing to fight the paste issue. Worth suggesting this pattern proactively if paste trouble recurs.

</attempted_approaches>

<critical_context>

## The corrected Property 3 table (exact, final, as written into FX-circle-properties.md)

| Line | From FX1's arm | From FX2's arm |
|---|---|---|
| Line 1 | 32 yang | 32 yin |
| Line 2 | 16 yang, 16 yin | 16 yin, 16 yang |
| Line 3 | 2×(8 yang, 8 yin) | 2×(8 yin, 8 yang) |
| Line 4 | 4×(4 yang, 4 yin) | 4×(4 yin, 4 yang) |
| Line 5 | 8×(2 yang, 2 yin) | 8×(2 yin, 2 yang) |
| Line 6 | 16×(yang, yin) | 16×(yin, yang) |

Both columns read outward counter-clockwise from their pole, **in the same rotational direction** — the relationship between the two columns at any matching step is direct full complementation (Observation 1 applied position-by-position), not a mirror/reversal.

## TOP_ARM / BOTTOM_ARM (exact, as embedded in FX-circle-unfolding.html)

```
TOP_ARM    = [15,13,11,9,7,5,3,1,31,29,27,25,23,21,19,17,47,45,43,41,39,37,35,33,63,61,59,57,55,53,51,49]
BOTTOM_ARM = [16,14,12,10,8,6,4,2,32,30,28,26,24,22,20,18,48,46,44,42,40,38,36,34,64,62,60,58,56,54,52,50]
```

Verified: `bottom[k]` is the exact 6-line complement of `top[k]` for every k = 0..31. Regeneration method (if this needs re-deriving): parse `diagrams/FX_circle_KW_square.html`'s 8 wedge `rotate()` transforms and all 64 glyph `<title>` angles, compute absolute angle = local + wedge rotation, sort by increasing angle, find the index where line 1 flips 0→1, rotate the 64-array to start there — first 32 = TOP_ARM, next 32 = BOTTOM_ARM.

## FX-circle-unfolding.html step schedule

193 total steps (indices 0–192). Step 0 = static vertical bar, nothing drawn. Steps 1–192: `pass = floor((step-1)/32)` (0-indexed, 0=line1...5=line6), `k = (step-1) % 32` (position within the pass, 0–31). Bar angle for the top arm at a given k: `92.8125 + 5.625×k` (92.8125° = 90° + the one-time 2.8125° alignment step); bottom arm is always `+180°` from that. Each pass resets to the same 32 angular positions and adds one new ring (radius `RING_R[pass]`) — nothing already drawn is ever erased.

## Relevant persistent memory

`feedback_verification-standard` (central to this whole session — both framing errors were caught precisely because claims were re-checked against fresh computation rather than trusted on sight), `feedback_handoff-staleness-check`, `feedback_whats-next-leads-with-todos` (followed — see `<reading_instructions>`), `feedback_push-reminder` (satisfied directly by this session's own closing instruction), `project_github-setup`, `reference_pcloud-backups` (directly relevant given the mac17 cross-machine resume — see `<work_remaining>`), `reference_obsidian-vault`, `project_asana-removed`, `user_extreme-zoom-out-viewing` (relevant to the ring-spacing fix — bigger/further-out was the right direction to push), `project_spine-page-css-gotcha` (not directly relevant this session).

</critical_context>

<current_state>

## Git status (as of writing this handoff, before step 3/commit)

```
$ git status --short
 M TO-DOS.md
A  notes/structure/FX-circle-properties.md
 M notes/structure/Group-Table-properties.md
?? diagrams/FX-circle-unfolding.html
```

(`whats-next.md` itself will also show as modified once this write completes. Re-run `git status` fresh before committing — don't trust this snapshot. Note `diagrams/FX-circle-unfolding.html` is **untracked**, not just modified — needs `git add diagrams/FX-circle-unfolding.html` explicitly, a plain `git add -u` would miss it.)

```
$ git log --oneline -5
ff9b69d Add trigram-pair perspective to Observation 11; update TODOs and handoff
38e3599 Add binary-sequence numbering comparison (Observation 11)
f292219 Add Klein four-group characterization of Group Table (Observation 10)
c803eea Make group-table distance badges selectable, add trigram-category observations
dfacacb Remove spine-side distance brackets, add KW-distance badges to group table
```

## Deliverable status

- **`notes/structure/FX-circle-properties.md`**: new file, complete for this session's scope (Observations 1–4 plus the antipodal-Gray-code aside). Heading structure verified. **Uncommitted (untracked-turned-staged, verify with `git status`).**
- **`notes/structure/Group-Table-properties.md`**: one small edit this session (duplicate aside replaced with a cross-link to the new file). **Uncommitted.**
- **`diagrams/FX-circle-unfolding.html`**: new, fully verified via headless-Chromium screenshots (mechanics + the mid-session spacing fix), not yet linked from other pages, not yet seen live by the user. **Uncommitted and untracked.**
- **`TO-DOS.md`**: one new top section, one UPDATE annotation. **Uncommitted.**
- **`whats-next.md`** (this file): freshly written this session per explicit user instruction (step 2 of 3).
- **All other project files**: untouched this session.

## Immediate next step for whoever/whatever resumes (very possibly on mac17, a different machine)

1. If resuming as a fresh session: greet/resume with the user, lead with `<current_todos>` as instructed.
2. **Run step 3 of the user's instruction: `git add` all four changed/new files listed above (remembering `FX-circle-unfolding.html` is untracked), commit, then push to `origin/master`.** Direct, already-given instruction from this same conversation turn.
3. If resuming on mac17 specifically: confirm the repo state there matches what this handoff describes (`git log`, `git status`) before assuming continuity — this is the first time this project has been picked up on a different machine mid-session per this project's own history, so don't assume the sync path is already proven reliable.
4. After push, everything in this session's own scope is complete; the highest-value next items are (a) confirming the FX1/FX2-vs-FX15/FX16 trade-off is acceptable, (b) linking `FX-circle-unfolding.html` from `index.html` and the other diagrams' footer navs, and (c) having the user actually click through the new animation live.

</current_state>
